# Фабрика сайтов v1 (вертикальный срез) — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `npm run dev` открывает в браузере форму, по кнопке «Сгенерировать» в `output/<домен>/` появляется готовый статический сайт, собранный выбранным шаблоном в выбранной цветовой схеме.

**Architecture:** три слоя с жёсткими границами. `factory/` — Express-сервер и статический UI, знает про папки и команды, не знает про вёрстку. `src/` — Astro-движок, чистая функция «контент + шаблон + схема → папка сайта», не знает про сервер и хостинги. `templates/tN/` — шаблоны-плагины: манифест и `.astro`-компоненты блоков; в коде движка ни один шаблон не упоминается по имени, подстановка идёт через `import.meta.glob` и манифест.

**Tech Stack:** Astro 7.3, Express 5.2, archiver 8, Vitest 5, чистый ESM без TypeScript.

**Спек:** `docs/specs/2026-09-07-factory-v1-design.md` — при расхождении плана и спека прав спек.

## Global Constraints

- Node.js ≥ 22.12.0 (требование Astro 7; нечётные версии вроде v23 не поддерживаются). Локально установлен v22.14.0.
- Пакетный менеджер — npm (локально 11.11.1). Файл блокировки `package-lock.json` коммитится.
- Только JavaScript (ESM, расширение `.mjs` для модулей вне Astro). TypeScript не подключается.
- **Валидации нет.** Никаких Zod и схем. Единственный случай отказа сборки со стороны данных — файл контента не читается или не является JSON.
- **Сборка не падает из-за контента.** Отсутствующее поле заполняется значением по умолчанию, неизвестный блок пропускается с предупреждением в лог.
- **Ноль клиентского JS в собираемых сайтах.** Ни один компонент не получает директиву `client:*`. JS допустим только в `factory/public/` — это интерфейс фабрики, а не сайт.
- **Движок не знает шаблонов по имени.** Строки `t1`, `t2`, `t3` не должны встречаться в `src/`. Шаблон приходит переменной окружения `TEMPLATE`.
- **Шрифты только системные.** Внешние шрифтовые сервисы не подключаются.
- Код и комментарии в коде — на английском. Пользовательские строки в UI фабрики — на русском, тексты сайтов приходят из контента.
- Git используется: каждая задача завершается коммитом. Ветка `main`, remote `origin` → `https://github.com/MaksVatrenko/site-factory.git`. Пуш — по завершении задачи.
- Сообщения коммитов на русском, в конце строка `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.
- Рабочая директория: `/Users/maksvatrenko/site-factory`.

## File Structure

```
package.json                  скрипты dev, test, build:site, check:matrix
astro.config.mjs              outDir, site, publicDir из переменных окружения
vitest.config.mjs             tests/**/*.test.mjs, увеличенный таймаут для сборочных тестов
public/.gitkeep               пустой publicDir по умолчанию

src/lib/normalize.mjs         normalizeSite() — приведение любого входа к рендерабельной форме
src/lib/templates.mjs         listTemplates(), readManifest(), loadTemplate(), missingBlockFiles()
src/lib/schemes.mjs           listSchemes(), readScheme()
src/lib/site-context.mjs      loadContext() — сборка контекста из переменных окружения
src/layouts/Base.astro        html/head, meta, canonical, инлайн base.css и схемы
src/components/BlockRenderer.astro   выбор компонента по типу блока и id шаблона
src/pages/[...slug].astro     getStaticPaths из site.pages
src/pages/sitemap.xml.js      статический эндпоинт
src/pages/robots.txt.js       статический эндпоинт

templates/t1/manifest.json    «Классический»
templates/t1/blocks/{hero,richtext,cards,columns,faq,footer}.astro
templates/t2/…                «Разделённый»
templates/t3/…                «Компактный»

styles/base.css               сброс и типографика на переменных схемы
styles/schemes/{blue,green,dark}.css   только :root-переменные

data/examples/default/site.json        английский пример, две страницы
data/examples/default/public/          картинки примера
data/examples/shop-ar/site.json        RTL-пример
output/<домен>/                        результат сборки (в git не попадает)

factory/server.mjs            createApp() и запуск: API, SSE-лог, zip, превью
factory/public/index.html     четыре вкладки, форма генерации
factory/public/app.js         загрузка списков, отправка формы, чтение лога
factory/public/styles.css     оформление интерфейса

scripts/check-matrix.mjs      ручная проверка всех 9 комбинаций шаблон × схема

tests/helpers/build.mjs       buildSite(), readOutput()
tests/smoke.test.mjs
tests/normalize.test.mjs
tests/schemes.test.mjs
tests/templates.test.mjs
tests/render.test.mjs
tests/seo.test.mjs
tests/server.test.mjs
```

---

### Task 1: Каркас проекта

**Files:**
- Create: `package.json`, `astro.config.mjs`, `vitest.config.mjs`, `public/.gitkeep`
- Test: `tests/smoke.test.mjs`

**Interfaces:**
- Consumes: ничего
- Produces: рабочие команды `npm test`; установленные Astro 7, Express 5, archiver 8, Vitest 5. Команда сборки заработает в задаче 5, когда появятся `src/pages/` и загрузчик контекста.

- [ ] **Step 1: Создать package.json**

```json
{
  "name": "site-factory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node factory/server.mjs",
    "build:site": "astro build",
    "test": "vitest run",
    "check:matrix": "node scripts/check-matrix.mjs"
  },
  "dependencies": {
    "archiver": "^8.0.0",
    "astro": "^7.3.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "vitest": "^5.0.0"
  }
}
```

- [ ] **Step 2: Установить зависимости**

Run: `npm install`
Expected: создаётся `node_modules/` и `package-lock.json`, ошибок нет.

- [ ] **Step 3: Проверить установленные версии**

Run: `npm ls astro express archiver vitest --depth=0`
Expected: astro 7.3.x, express 5.2.x, archiver 8.0.x, vitest 5.0.x. Если мажорная версия отличается — остановиться и сообщить, план рассчитан на эти мажоры.

- [ ] **Step 4: Создать astro.config.mjs**

Три переменные окружения управляют сборкой. `publicDir` указывает на папку примера, чтобы картинки контента попали в корень сайта; если её нет — берётся пустая `./public`.

```javascript
import { existsSync } from 'node:fs';
import { defineConfig } from 'astro/config';

const envPublicDir = process.env.PUBLIC_DIR;
const publicDir = envPublicDir && existsSync(envPublicDir) ? envPublicDir : './public';

export default defineConfig({
  site: process.env.SITE_URL || 'https://example.com',
  outDir: process.env.OUT_DIR || './output/preview',
  publicDir,
  build: { format: 'directory' },
  devToolbar: { enabled: false },
});
```

- [ ] **Step 5: Создать vitest.config.mjs**

Сборочные тесты запускают Astro дочерним процессом, поэтому таймаут увеличен. `fileParallelism: false` не даёт двум сборкам писать в один каталог одновременно.

```javascript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    include: ['tests/**/*.test.mjs'],
    testTimeout: 180000,
    hookTimeout: 180000,
    fileParallelism: false,
  },
});
```

- [ ] **Step 6: Создать пустой publicDir по умолчанию**

Run: `mkdir -p public && touch public/.gitkeep`
Expected: файл `public/.gitkeep` существует.

- [ ] **Step 7: Написать smoke-тест**

Файл `tests/smoke.test.mjs`:

```javascript
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';

describe('project skeleton', () => {
  it('runs on a Node version Astro 7 supports', () => {
    const [major, minor] = process.versions.node.split('.').map(Number);
    expect(major).toBeGreaterThanOrEqual(22);
    if (major === 22) expect(minor).toBeGreaterThanOrEqual(12);
    expect(major % 2).toBe(0);
  });

  it('declares the expected scripts', () => {
    const pkg = JSON.parse(readFileSync('package.json', 'utf8'));
    expect(Object.keys(pkg.scripts)).toEqual(
      expect.arrayContaining(['dev', 'test', 'build:site', 'check:matrix']),
    );
    expect(pkg.type).toBe('module');
  });
});
```

- [ ] **Step 8: Прогнать тест**

Run: `npm test`
Expected: PASS, 2 теста.

- [ ] **Step 9: Коммит**

```bash
git add package.json package-lock.json astro.config.mjs vitest.config.mjs public/.gitkeep tests/smoke.test.mjs
git commit -m "$(cat <<'MSG'
Каркас проекта: Astro 7, Express 5, Vitest 5

Конфигурация сборки читает outDir, site и publicDir из переменных окружения,
что делает движок пригодным для запуска из фабрики.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
)"
git push
```

---

### Task 2: Нормализатор контента

Заменяет собой Zod-валидацию. Функция принимает что угодно и возвращает структуру, которую движок гарантированно может отрендерить, плюс список предупреждений.

**Files:**
- Create: `src/lib/normalize.mjs`
- Test: `tests/normalize.test.mjs`

**Interfaces:**
- Consumes: ничего
- Produces: `normalizeSite(raw, options) → { site, warnings }`, где
  - `raw` — любое значение, разобранное из JSON;
  - `options.supportedBlocks` — массив строк или `undefined` (тогда типы блоков не фильтруются);
  - `options.overrides` — объект с полями `brand`, `locale`, `geo`, `partnerUrl`, `domain`, `style`; пустая строка означает «не переопределять»;
  - `site` — `{ domain, locale, lang, dir, geo, partnerUrl, style, brand: { name, logo }, pages }`;
  - `page` — `{ slug, meta: { title, description }, blocks }`;
  - `block` — `{ type, props }`;
  - `warnings` — массив строк на русском.

- [ ] **Step 1: Написать падающий тест**

Файл `tests/normalize.test.mjs`:

```javascript
import { describe, it, expect } from 'vitest';
import { normalizeSite } from '../src/lib/normalize.mjs';

const BLOCKS = ['hero', 'richtext', 'cards', 'columns', 'faq', 'footer'];

describe('normalizeSite', () => {
  it('builds an empty home page when there are no pages', () => {
    const { site, warnings } = normalizeSite({}, { supportedBlocks: BLOCKS });
    expect(site.pages).toHaveLength(1);
    expect(site.pages[0].slug).toBe('/');
    expect(warnings.join(' ')).toContain('нет страниц');
  });

  it('accepts a non-object root without throwing', () => {
    const { site } = normalizeSite('broken', { supportedBlocks: BLOCKS });
    expect(site.pages).toHaveLength(1);
    expect(site.brand.name).toBe('Site');
  });

  it('wraps a single page object into an array', () => {
    const { site } = normalizeSite({ pages: { slug: '/' } }, { supportedBlocks: BLOCKS });
    expect(site.pages).toHaveLength(1);
  });

  it('gives every block a props object', () => {
    const { site } = normalizeSite(
      { pages: [{ blocks: [{ type: 'hero' }] }] },
      { supportedBlocks: BLOCKS },
    );
    expect(site.pages[0].blocks[0].props).toEqual({});
  });

  it('drops blocks the template does not support', () => {
    const { site, warnings } = normalizeSite(
      { pages: [{ blocks: [{ type: 'hero' }, { type: 'carousel' }] }] },
      { supportedBlocks: BLOCKS },
    );
    const types = site.pages[0].blocks.map((b) => b.type);
    expect(types).toContain('hero');
    expect(types).not.toContain('carousel');
    expect(warnings.join(' ')).toContain('carousel');
  });

  it('drops blocks without a type', () => {
    const { site, warnings } = normalizeSite(
      { pages: [{ blocks: [{ props: {} }, 'nonsense'] }] },
      { supportedBlocks: BLOCKS },
    );
    expect(site.pages[0].blocks.filter((b) => b.type !== 'footer')).toHaveLength(0);
    expect(warnings.length).toBeGreaterThan(0);
  });

  it('falls back to the brand name for a missing title', () => {
    const { site } = normalizeSite(
      { brand: { name: 'Acme' }, pages: [{ slug: '/' }] },
      { supportedBlocks: BLOCKS },
    );
    expect(site.pages[0].meta.title).toBe('Acme');
  });

  it('normalizes slugs to a single leading slash', () => {
    const { site } = normalizeSite(
      { pages: [{ slug: '/' }, { slug: 'about/' }, { slug: '//guide' }] },
      { supportedBlocks: BLOCKS },
    );
    expect(site.pages.map((p) => p.slug)).toEqual(['/', '/about', '/guide']);
  });

  it('lets overrides win over file values', () => {
    const { site } = normalizeSite(
      { domain: 'from-file.com', brand: { name: 'FromFile' }, locale: 'en-US' },
      {
        supportedBlocks: BLOCKS,
        overrides: { brand: 'FromForm', domain: 'from-form.com', geo: 'ID' },
      },
    );
    expect(site.brand.name).toBe('FromForm');
    expect(site.domain).toBe('from-form.com');
    expect(site.geo).toBe('ID');
    expect(site.locale).toBe('en-US');
  });

  it('ignores empty overrides', () => {
    const { site } = normalizeSite(
      { brand: { name: 'FromFile' } },
      { supportedBlocks: BLOCKS, overrides: { brand: '' } },
    );
    expect(site.brand.name).toBe('FromFile');
  });

  it('marks right-to-left locales', () => {
    const ltr = normalizeSite({ locale: 'en-US' }, { supportedBlocks: BLOCKS });
    const rtl = normalizeSite({ locale: 'ar-AE' }, { supportedBlocks: BLOCKS });
    expect(ltr.site.dir).toBe('ltr');
    expect(ltr.site.lang).toBe('en');
    expect(rtl.site.dir).toBe('rtl');
    expect(rtl.site.lang).toBe('ar');
  });

  it('appends a footer block when the template supports one', () => {
    const { site } = normalizeSite(
      { pages: [{ blocks: [{ type: 'hero' }] }] },
      { supportedBlocks: BLOCKS },
    );
    expect(site.pages[0].blocks.at(-1).type).toBe('footer');
  });

  it('does not append a footer the template cannot render', () => {
    const { site } = normalizeSite(
      { pages: [{ blocks: [{ type: 'hero' }] }] },
      { supportedBlocks: ['hero'] },
    );
    expect(site.pages[0].blocks.map((b) => b.type)).toEqual(['hero']);
  });

  it('never appends a second footer', () => {
    const { site } = normalizeSite(
      { pages: [{ blocks: [{ type: 'hero' }, { type: 'footer' }] }] },
      { supportedBlocks: BLOCKS },
    );
    expect(site.pages[0].blocks.filter((b) => b.type === 'footer')).toHaveLength(1);
  });
});
```

- [ ] **Step 2: Запустить тест и убедиться, что он падает**

Run: `npx vitest run tests/normalize.test.mjs`
Expected: FAIL — `Failed to load ../src/lib/normalize.mjs`.

- [ ] **Step 3: Написать нормализатор**

Файл `src/lib/normalize.mjs`:

```javascript
const RTL_LANGUAGES = new Set(['ar', 'fa', 'he', 'ur']);
const DEFAULT_BRAND = 'Site';
const DEFAULT_DOMAIN = 'example.com';
const DEFAULT_LOCALE = 'en-US';

function isPlainObject(value) {
  return typeof value === 'object' && value !== null && !Array.isArray(value);
}

function toArray(value) {
  if (Array.isArray(value)) return value;
  if (value === undefined || value === null || value === '') return [];
  return [value];
}

function toText(value, fallback = '') {
  return typeof value === 'string' && value.trim() !== '' ? value : fallback;
}

function normalizeSlug(value, index) {
  const raw = toText(value, index === 0 ? '/' : `page-${index}`);
  const trimmed = raw.replace(/^\/+/, '').replace(/\/+$/, '');
  return trimmed === '' ? '/' : `/${trimmed}`;
}

function pickOverride(override, fromFile, fallback) {
  return toText(override, toText(fromFile, fallback));
}

export function normalizeSite(raw, options = {}) {
  const warnings = [];
  const overrides = options.overrides || {};
  const supported = Array.isArray(options.supportedBlocks)
    ? new Set(options.supportedBlocks)
    : null;

  let input = raw;
  if (!isPlainObject(input)) {
    warnings.push('Корень файла контента не объект — взяты значения по умолчанию');
    input = {};
  }

  const brandInput = isPlainObject(input.brand) ? input.brand : {};
  const brand = {
    name: pickOverride(overrides.brand, brandInput.name, DEFAULT_BRAND),
    logo: toText(brandInput.logo, ''),
  };

  const locale = pickOverride(overrides.locale, input.locale, DEFAULT_LOCALE);
  const lang = locale.split(/[-_]/)[0].toLowerCase();

  const site = {
    domain: pickOverride(overrides.domain, input.domain, DEFAULT_DOMAIN),
    locale,
    lang,
    dir: RTL_LANGUAGES.has(lang) ? 'rtl' : 'ltr',
    geo: pickOverride(overrides.geo, input.geo, ''),
    partnerUrl: pickOverride(overrides.partnerUrl, input.partnerUrl, ''),
    style: pickOverride(overrides.style, input.style, ''),
    brand,
    pages: [],
  };

  let pages = toArray(input.pages).filter(isPlainObject);
  if (pages.length === 0) {
    warnings.push('В контенте нет страниц — создана пустая главная страница');
    pages = [{}];
  }

  site.pages = pages.map((page, index) => {
    const slug = normalizeSlug(page.slug, index);
    const meta = isPlainObject(page.meta) ? page.meta : {};
    const blocks = [];

    for (const candidate of toArray(page.blocks)) {
      if (!isPlainObject(candidate)) {
        warnings.push(`${slug}: пропущен блок — это не объект`);
        continue;
      }
      const type = toText(candidate.type, '');
      if (type === '') {
        warnings.push(`${slug}: пропущен блок без поля type`);
        continue;
      }
      if (supported && !supported.has(type)) {
        warnings.push(`${slug}: шаблон не поддерживает блок «${type}» — пропущен`);
        continue;
      }
      blocks.push({
        type,
        props: isPlainObject(candidate.props) ? candidate.props : {},
      });
    }

    const canRenderFooter = !supported || supported.has('footer');
    if (canRenderFooter && !blocks.some((block) => block.type === 'footer')) {
      blocks.push({ type: 'footer', props: {} });
    }

    return {
      slug,
      meta: {
        title: toText(meta.title, brand.name),
        description: toText(meta.description, ''),
      },
      blocks,
    };
  });

  return { site, warnings };
}
```

- [ ] **Step 4: Запустить тесты**

Run: `npx vitest run tests/normalize.test.mjs`
Expected: PASS, 14 тестов.

- [ ] **Step 5: Прогнать весь набор**

Run: `npm test`
Expected: PASS.

- [ ] **Step 6: Коммит**

```bash
git add src/lib/normalize.mjs tests/normalize.test.mjs
git commit -m "$(cat <<'MSG'
Нормализатор контента вместо валидации

Любой вход приводится к рендерабельной форме: недостающие поля получают
значения по умолчанию, неподдерживаемые блоки отбрасываются с предупреждением.
Сборка не может упасть из-за кривого контента.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
)"
git push
```

---

### Task 3: Базовые стили и цветовые схемы

**Files:**
- Create: `styles/base.css`, `styles/schemes/blue.css`, `styles/schemes/green.css`, `styles/schemes/dark.css`, `src/lib/schemes.mjs`
- Test: `tests/schemes.test.mjs`

**Interfaces:**
- Consumes: ничего
- Produces:
  - `listSchemes(root?) → string[]` — идентификаторы схем (имена файлов без `.css`), отсортированы по алфавиту;
  - `readScheme(id, root?) → { id, css, fellBack }` — `id` фактически выбранной схемы, её CSS текстом, `fellBack === true` если запрошенной схемы не нашлось и взята первая по алфавиту;
  - набор CSS-переменных `--c-bg`, `--c-fg`, `--c-surface`, `--c-border`, `--c-muted`, `--c-primary`, `--c-primary-fg`, `--c-accent`, на которые опираются все шаблоны;
  - классы `.container`, `.section`, `.button` из `base.css`, используемые компонентами блоков.

- [ ] **Step 1: Написать падающий тест**

Файл `tests/schemes.test.mjs`:

```javascript
import { describe, it, expect } from 'vitest';
import { listSchemes, readScheme } from '../src/lib/schemes.mjs';

function variablesOf(css) {
  return [...css.matchAll(/(--[a-z0-9-]+)\s*:/g)].map((match) => match[1]).sort();
}

describe('colour schemes', () => {
  it('lists every scheme file in the folder', () => {
    expect(listSchemes()).toEqual(['blue', 'dark', 'green']);
  });

  it('reads the requested scheme', () => {
    const scheme = readScheme('green');
    expect(scheme.id).toBe('green');
    expect(scheme.fellBack).toBe(false);
    expect(scheme.css).toContain('--c-primary');
  });

  it('falls back to the first scheme when the id is unknown', () => {
    const scheme = readScheme('chartreuse');
    expect(scheme.id).toBe('blue');
    expect(scheme.fellBack).toBe(true);
  });

  it('falls back when no id is given at all', () => {
    expect(readScheme('').id).toBe('blue');
    expect(readScheme(undefined).id).toBe('blue');
  });

  it('defines the same variables in every scheme', () => {
    const [first, ...rest] = listSchemes();
    const expected = variablesOf(readScheme(first).css);
    expect(expected.length).toBeGreaterThan(5);
    for (const id of rest) {
      expect(variablesOf(readScheme(id).css)).toEqual(expected);
    }
  });
});
```

- [ ] **Step 2: Запустить тест и убедиться, что он падает**

Run: `npx vitest run tests/schemes.test.mjs`
Expected: FAIL — модуль `../src/lib/schemes.mjs` не найден.

- [ ] **Step 3: Написать три схемы**

Файл `styles/schemes/blue.css`:

```css
:root {
  --c-bg: #ffffff;
  --c-fg: #16202f;
  --c-surface: #f1f5f9;
  --c-border: #dbe3ec;
  --c-muted: #5b6b80;
  --c-primary: #2563eb;
  --c-primary-fg: #ffffff;
  --c-accent: #1d4ed8;
}
```

Файл `styles/schemes/green.css`:

```css
:root {
  --c-bg: #ffffff;
  --c-fg: #14231b;
  --c-surface: #eef6f0;
  --c-border: #d5e6da;
  --c-muted: #4f6a59;
  --c-primary: #10794a;
  --c-primary-fg: #ffffff;
  --c-accent: #0b5c37;
}
```

Файл `styles/schemes/dark.css`:

```css
:root {
  --c-bg: #0f141c;
  --c-fg: #e8eef7;
  --c-surface: #18202b;
  --c-border: #2a3646;
  --c-muted: #9aa9bd;
  --c-primary: #4d97ff;
  --c-primary-fg: #08111c;
  --c-accent: #7db4ff;
}
```

- [ ] **Step 4: Написать базовые стили**

Файл `styles/base.css`. Ни одного цвета напрямую — только переменные схемы, поэтому смена схемы перекрашивает сайт целиком.

```css
*,
*::before,
*::after {
  box-sizing: border-box;
}

* {
  margin: 0;
}

:root {
  --radius: 12px;
  --radius-sm: 8px;
  --space: clamp(2.5rem, 6vw, 4.5rem);
}

html {
  -webkit-text-size-adjust: 100%;
}

body {
  background: var(--c-bg);
  color: var(--c-fg);
  font-family: system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue',
    Arial, 'Noto Sans', sans-serif;
  font-size: 16px;
  line-height: 1.65;
}

img,
picture,
svg {
  display: block;
  max-width: 100%;
  height: auto;
}

h1,
h2,
h3 {
  line-height: 1.18;
  text-wrap: balance;
}

h1 {
  font-size: clamp(2rem, 5vw, 3.25rem);
}

h2 {
  font-size: clamp(1.5rem, 3.5vw, 2.25rem);
}

h3 {
  font-size: 1.2rem;
}

p {
  text-wrap: pretty;
}

a {
  color: var(--c-primary);
}

ul {
  padding-inline-start: 1.1rem;
}

.container {
  width: min(100% - 2.5rem, 1120px);
  margin-inline: auto;
}

.shell {
  width: min(100% - 2.5rem, 44rem);
  margin-inline: auto;
}

.section {
  padding-block: var(--space);
}

.section--surface {
  background: var(--c-surface);
}

.button {
  display: inline-block;
  padding: 0.85rem 1.9rem;
  border-radius: var(--radius-sm);
  background: var(--c-primary);
  color: var(--c-primary-fg);
  font-weight: 600;
  text-decoration: none;
}

.eyebrow {
  color: var(--c-muted);
  font-size: 0.85rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}
```

- [ ] **Step 5: Написать реестр схем**

Файл `src/lib/schemes.mjs`:

```javascript
import { existsSync, readdirSync, readFileSync } from 'node:fs';
import { join } from 'node:path';

export const SCHEMES_DIR = join('styles', 'schemes');

export function listSchemes(root = process.cwd()) {
  const dir = join(root, SCHEMES_DIR);
  if (!existsSync(dir)) return [];
  return readdirSync(dir)
    .filter((name) => name.endsWith('.css'))
    .map((name) => name.slice(0, -'.css'.length))
    .sort();
}

export function readScheme(id, root = process.cwd()) {
  const available = listSchemes(root);
  if (available.length === 0) {
    throw new Error(`No colour schemes found in ${SCHEMES_DIR}`);
  }
  const wanted = typeof id === 'string' ? id : '';
  const chosen = available.includes(wanted) ? wanted : available[0];
  return {
    id: chosen,
    css: readFileSync(join(root, SCHEMES_DIR, `${chosen}.css`), 'utf8'),
    fellBack: chosen !== wanted,
  };
}
```

- [ ] **Step 6: Запустить тесты**

Run: `npx vitest run tests/schemes.test.mjs`
Expected: PASS, 5 тестов.

- [ ] **Step 7: Прогнать весь набор**

Run: `npm test`
Expected: PASS.

- [ ] **Step 8: Коммит**

```bash
git add styles src/lib/schemes.mjs tests/schemes.test.mjs
git commit -m "$(cat <<'MSG'
Базовые стили и три цветовые схемы

Схема — только :root-переменные, базовые стили не содержат ни одного цвета
напрямую. Список схем строится сканированием папки, а не захардкожен.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
)"
git push
```

---

### Task 4: Реестр шаблонов

Здесь появляется первый шаблон в минимальном составе — двух блоков хватает, чтобы движок из задачи 5 собрал страницу. Остальные блоки добавляет задача 6.

**Files:**
- Create: `src/lib/templates.mjs`, `templates/t1/manifest.json`, `templates/t1/blocks/hero.astro`, `templates/t1/blocks/footer.astro`
- Test: `tests/templates.test.mjs`

**Interfaces:**
- Consumes: ничего
- Produces:
  - `listTemplates(root?) → Manifest[]`, отсортированы по `id`;
  - `readManifest(id, root?) → Manifest`, бросает ошибку если манифеста нет;
  - `loadTemplate(id, root?) → Manifest` — запрошенный шаблон или первый по алфавиту, если запрошенного нет;
  - `missingBlockFiles(manifest, root?) → string[]` — типы блоков, заявленные в манифесте, для которых нет файла;
  - `Manifest` = `{ id, name, description, blocks: string[], defaultScheme: string }`.

- [ ] **Step 1: Написать падающий тест**

Файл `tests/templates.test.mjs`:

```javascript
import { describe, it, expect } from 'vitest';
import {
  listTemplates,
  readManifest,
  loadTemplate,
  missingBlockFiles,
} from '../src/lib/templates.mjs';

describe('template registry', () => {
  it('finds at least one template', () => {
    const templates = listTemplates();
    expect(templates.length).toBeGreaterThan(0);
    expect(templates[0]).toMatchObject({
      id: expect.any(String),
      name: expect.any(String),
      blocks: expect.any(Array),
    });
  });

  it('keeps templates sorted by id', () => {
    const ids = listTemplates().map((template) => template.id);
    expect(ids).toEqual([...ids].sort());
  });

  it('reads a manifest by id', () => {
    const manifest = readManifest('t1');
    expect(manifest.id).toBe('t1');
    expect(manifest.blocks).toContain('hero');
    expect(manifest.defaultScheme).toBeTruthy();
  });

  it('throws for an unknown manifest', () => {
    expect(() => readManifest('nope')).toThrow(/nope/);
  });

  it('falls back to the first template for an unknown id', () => {
    expect(loadTemplate('nope').id).toBe(listTemplates()[0].id);
    expect(loadTemplate('').id).toBe(listTemplates()[0].id);
  });

  it('reports no missing files for shipped templates', () => {
    for (const template of listTemplates()) {
      expect(missingBlockFiles(template)).toEqual([]);
    }
  });

  it('reports a block file that does not exist', () => {
    const broken = { id: 't1', blocks: ['hero', 'imaginary'] };
    expect(missingBlockFiles(broken)).toEqual(['imaginary']);
  });
});
```

- [ ] **Step 2: Запустить тест и убедиться, что он падает**

Run: `npx vitest run tests/templates.test.mjs`
Expected: FAIL — модуль `../src/lib/templates.mjs` не найден.

- [ ] **Step 3: Написать реестр**

Файл `src/lib/templates.mjs`:

```javascript
import { existsSync, readdirSync, readFileSync } from 'node:fs';
import { join } from 'node:path';

export const TEMPLATES_DIR = 'templates';

function manifestPath(id, root) {
  return join(root, TEMPLATES_DIR, id, 'manifest.json');
}

export function readManifest(id, root = process.cwd()) {
  const file = manifestPath(id, root);
  if (!existsSync(file)) {
    throw new Error(`Template "${id}" not found: no manifest at ${file}`);
  }
  const raw = JSON.parse(readFileSync(file, 'utf8'));
  return {
    id: typeof raw.id === 'string' && raw.id !== '' ? raw.id : id,
    name: typeof raw.name === 'string' && raw.name !== '' ? raw.name : id,
    description: typeof raw.description === 'string' ? raw.description : '',
    blocks: Array.isArray(raw.blocks) ? raw.blocks.filter((b) => typeof b === 'string') : [],
    defaultScheme: typeof raw.defaultScheme === 'string' ? raw.defaultScheme : '',
  };
}

export function listTemplates(root = process.cwd()) {
  const dir = join(root, TEMPLATES_DIR);
  if (!existsSync(dir)) return [];
  return readdirSync(dir)
    .filter((name) => existsSync(manifestPath(name, root)))
    .map((name) => readManifest(name, root))
    .sort((a, b) => a.id.localeCompare(b.id));
}

export function loadTemplate(id, root = process.cwd()) {
  const available = listTemplates(root);
  if (available.length === 0) {
    throw new Error(`No templates found in ${TEMPLATES_DIR}`);
  }
  return available.find((template) => template.id === id) ?? available[0];
}

export function missingBlockFiles(manifest, root = process.cwd()) {
  const dir = join(root, TEMPLATES_DIR, manifest.id, 'blocks');
  return manifest.blocks.filter((type) => !existsSync(join(dir, `${type}.astro`)));
}
```

- [ ] **Step 4: Создать манифест первого шаблона**

Файл `templates/t1/manifest.json`. Пока объявлены только два блока — ровно те, файлы которых существуют; иначе `missingBlockFiles` справедливо забракует шаблон.

```json
{
  "id": "t1",
  "name": "Классический",
  "description": "Широкий hero по центру, карточки сеткой три в ряд",
  "blocks": ["hero", "footer"],
  "defaultScheme": "blue"
}
```

- [ ] **Step 5: Написать блок hero**

Файл `templates/t1/blocks/hero.astro`. Все блоки получают одинаковые пропсы: разложенный `props` блока плюс `site` — общий контекст сайта.

```astro
---
import { asText } from '../../_shared/props.mjs';

const { title, subtitle, cta, image, site } = Astro.props;
const heading = asText(title, site?.brand?.name || 'Заголовок');
const subtitleText = asText(subtitle);
const buttonLabel = asText(cta, 'Подробнее');
const buttonHref = site?.partnerUrl;
const imageSrc = asText(image);
---

<section class="hero section">
  <div class="container hero__inner">
    {site?.brand?.name && <p class="eyebrow">{site.brand.name}</p>}
    <h1>{heading}</h1>
    {subtitleText && <p class="hero__subtitle">{subtitleText}</p>}
    {site?.partnerUrl && <a class="button" href={buttonHref}>{buttonLabel}</a>}
    {imageSrc && <img class="hero__image" src={imageSrc} alt="" loading="eager" />}
  </div>
</section>
<style>
  .hero {
    background: var(--c-surface);
    text-align: center;
  }

  .hero__inner {
    display: grid;
    justify-items: center;
    gap: 1.25rem;
    max-width: 46rem;
  }

  .hero__subtitle {
    color: var(--c-muted);
    font-size: 1.15rem;
  }

  .hero__image {
    margin-top: 1rem;
    border-radius: var(--radius);
  }
</style>
```

- [ ] **Step 6: Написать блок footer**

Файл `templates/t1/blocks/footer.astro`:

```astro
---
const { site } = Astro.props;
const year = new Date().getFullYear();
const disclaimer =
  'Материалы носят информационный характер и не являются публичной офертой.';
---

<footer class="footer section">
  <div class="container footer__inner">
    <p class="footer__brand">{site?.brand?.name}</p>
    <p class="footer__disclaimer">{disclaimer}</p>
    <p class="footer__copy">© {year} {site?.domain}</p>
  </div>
</footer>

<style>
  .footer {
    border-top: 1px solid var(--c-border);
    padding-block: 2.5rem;
  }

  .footer__inner {
    display: grid;
    gap: 0.6rem;
  }

  .footer__brand {
    font-weight: 700;
  }

  .footer__disclaimer,
  .footer__copy {
    color: var(--c-muted);
    font-size: 0.9rem;
  }
</style>
```

- [ ] **Step 7: Запустить тесты**

Run: `npx vitest run tests/templates.test.mjs`
Expected: PASS, 7 тестов.

- [ ] **Step 8: Прогнать весь набор**

Run: `npm test`
Expected: PASS.

- [ ] **Step 9: Коммит**

```bash
git add src/lib/templates.mjs templates/t1 tests/templates.test.mjs
git commit -m "$(cat <<'MSG'
Реестр шаблонов и первый шаблон в минимальном составе

Шаблон описывается манифестом; движок узнаёт о нём сканированием папки.
missingBlockFiles ловит расхождение между манифестом и файлами на диске.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
)"
git push
```

---

### Task 5: Движок — контекст, макет, маршрут

Первая сборка. После этой задачи команда в терминале превращает контент в готовый сайт; интерфейса ещё нет.

**Files:**
- Create: `src/lib/site-context.mjs`, `src/layouts/Base.astro`, `src/components/BlockRenderer.astro`, `src/pages/[...slug].astro`, `data/examples/default/site.json`, `data/examples/default/public/images/logo.svg`, `data/examples/broken/site.json`, `tests/helpers/build.mjs`
- Test: `tests/render.test.mjs`

**Interfaces:**
- Consumes: `normalizeSite()` (задача 2), `readScheme()` (задача 3), `loadTemplate()`, `missingBlockFiles()` (задача 4)
- Produces:
  - `loadContext(root?) → { site, template, scheme }` — читает переменные `SITE_JSON`, `TEMPLATE`, `SCHEME`, `BRAND`, `LOCALE`, `GEO`, `DOMAIN`, `PARTNER_URL`; результат кэшируется на процесс, потому что Astro вызывает его для каждой страницы;
  - контракт блока: компонент получает все поля `block.props` как пропсы плюс `site`;
  - `buildSite({ example, template, scheme, outDir, env }) → { outDir, log }` — тестовый хелпер, запускает Astro дочерним процессом;
  - `readOutput(outDir, file?) → string`.

- [ ] **Step 1: Создать пример контента**

Файл `data/examples/default/site.json`. Две страницы, все шесть типов блоков — часть из них t1 пока не поддерживает и отбросит; задача 6 это исправит.

```json
{
  "domain": "example.com",
  "locale": "en-US",
  "brand": { "name": "Example Guide", "logo": "images/logo.svg" },
  "pages": [
    {
      "slug": "/",
      "meta": {
        "title": "Example Guide — honest reviews",
        "description": "Independent reviews, comparisons and answers to common questions."
      },
      "blocks": [
        {
          "type": "hero",
          "props": {
            "title": "Find what actually works",
            "subtitle": "We test, compare and explain — so you do not have to guess.",
            "cta": "Start reading"
          }
        },
        {
          "type": "richtext",
          "props": {
            "heading": "Why this guide exists",
            "paragraphs": [
              "Most comparison pages repeat the same marketing copy. We do the opposite: every claim here comes from a test we ran ourselves.",
              "Nothing on this page is sponsored. When something is bad, we say so plainly."
            ]
          }
        },
        {
          "type": "cards",
          "props": {
            "heading": "What we check",
            "items": [
              { "title": "Speed", "text": "How long a first result takes on a cold start." },
              { "title": "Price", "text": "Total cost after the trial ends, not the headline number." },
              { "title": "Support", "text": "Response time measured over two weeks of real tickets." }
            ]
          }
        },
        {
          "type": "columns",
          "props": {
            "heading": "Browse by topic",
            "items": [
              { "title": "Getting started", "links": ["First steps", "Common mistakes", "Checklist"] },
              { "title": "Comparisons", "links": ["Head to head", "By budget", "By use case"] },
              { "title": "Advanced", "links": ["Automation", "Integrations", "Migration"] }
            ]
          }
        },
        {
          "type": "faq",
          "props": {
            "heading": "Questions we get a lot",
            "items": [
              { "q": "How often is this updated?", "a": "Every claim is re-tested at least once a quarter." },
              { "q": "Do you accept paid placements?", "a": "No. Rankings cannot be bought." },
              { "q": "Can I suggest a product?", "a": "Yes — send it over and it joins the testing queue." }
            ]
          }
        }
      ]
    },
    {
      "slug": "/about",
      "meta": { "title": "About Example Guide", "description": "Who runs this site and how it is funded." },
      "blocks": [
        {
          "type": "richtext",
          "props": {
            "heading": "About us",
            "paragraphs": [
              "A small team that got tired of comparison sites that never compare anything.",
              "The site is funded by affiliate links, which never affect the order of results."
            ]
          }
        }
      ]
    }
  ]
}
```

- [ ] **Step 2: Создать логотип примера**

Файл `data/examples/default/public/images/logo.svg`. Картинка нужна, чтобы проверить: содержимое `public/` примера попадает в корень собранного сайта.

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 32" width="120" height="32">
  <rect width="120" height="32" rx="6" fill="currentColor" opacity="0.12"/>
  <text x="12" y="21" font-family="system-ui, sans-serif" font-size="14" font-weight="700" fill="currentColor">Example</text>
</svg>
```

- [ ] **Step 3: Создать заведомо битый пример**

Файл `data/examples/broken/site.json`. Проверяет обещание «сборка не падает из-за контента»: здесь нет страниц с блоками нормального вида, есть блок без типа, блок-строка и блок несуществующего типа.

```json
{
  "brand": { "name": "Broken Example" },
  "pages": [
    {
      "slug": "sloppy//",
      "blocks": [
        { "type": "hero" },
        { "props": { "title": "no type at all" } },
        "just a string",
        { "type": "carousel", "props": { "items": [] } }
      ]
    }
  ]
}
```

- [ ] **Step 4: Написать загрузчик контекста**

Файл `src/lib/site-context.mjs`:

```javascript
import { readFileSync } from 'node:fs';
import { normalizeSite } from './normalize.mjs';
import { loadTemplate, missingBlockFiles } from './templates.mjs';
import { readScheme } from './schemes.mjs';

let cache = null;

export function resetContextCache() {
  cache = null;
}

export function loadContext(root = process.cwd()) {
  if (cache) return cache;

  const file = process.env.SITE_JSON;
  if (!file) {
    throw new Error('SITE_JSON is not set: point it at a content file');
  }

  let raw;
  try {
    raw = JSON.parse(readFileSync(file, 'utf8'));
  } catch (error) {
    throw new Error(`Cannot read content from ${file}: ${error.message}`);
  }

  const template = loadTemplate(process.env.TEMPLATE || '', root);
  const missing = missingBlockFiles(template, root);
  if (missing.length > 0) {
    throw new Error(
      `Template "${template.id}" declares blocks with no file: ${missing.join(', ')}`,
    );
  }

  const { site, warnings } = normalizeSite(raw, {
    supportedBlocks: template.blocks,
    overrides: {
      brand: process.env.BRAND || '',
      locale: process.env.LOCALE || '',
      geo: process.env.GEO || '',
      domain: process.env.DOMAIN || '',
      partnerUrl: process.env.PARTNER_URL || '',
    },
  });

  const scheme = readScheme(process.env.SCHEME || site.style || template.defaultScheme, root);

  for (const warning of warnings) console.warn(`[factory] ${warning}`);
  if (scheme.fellBack) {
    console.warn(`[factory] Схема не найдена, взята «${scheme.id}»`);
  }

  cache = { site, template, scheme };
  return cache;
}
```

- [ ] **Step 5: Написать макет**

Файл `src/layouts/Base.astro`. `?raw` заставляет Vite отдать содержимое CSS строкой — вместе с `set:html` это кладёт стили прямо в `<head>` без единого внешнего запроса.

```astro
---
import baseCss from '../../styles/base.css?raw';

const { site, page, scheme } = Astro.props;
const canonical = new URL(page.slug, Astro.site ?? `https://${site.domain}`).href;
---

<html lang={site.lang} dir={site.dir}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{page.meta.title}</title>
    {page.meta.description && <meta name="description" content={page.meta.description} />}
    <link rel="canonical" href={canonical} />
    {site.geo && <meta name="geo.region" content={site.geo} />}
    <meta property="og:title" content={page.meta.title} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonical} />
    <style set:html={`${baseCss}\n${scheme.css}`}></style>
  </head>
  <body>
    <slot />
  </body>
</html>
```

- [ ] **Step 6: Написать BlockRenderer**

Файл `src/components/BlockRenderer.astro`. Здесь и живёт граница «движок не знает шаблонов»: путь собирается из id шаблона и типа блока, ни одна из этих строк в коде не зашита.

```astro
---
const blockModules = import.meta.glob('../../templates/*/blocks/*.astro', { eager: true });

const { block, site, templateId } = Astro.props;
const key = `../../templates/${templateId}/blocks/${block.type}.astro`;
const Component = blockModules[key]?.default;
---

{Component && <Component {...block.props} site={site} />}
```

- [ ] **Step 7: Написать маршрут**

Файл `src/pages/[...slug].astro`. Для главной страницы rest-параметр равен `undefined` — так Astro отдаёт корень сайта.

```astro
---
import Base from '../layouts/Base.astro';
import BlockRenderer from '../components/BlockRenderer.astro';
import { loadContext } from '../lib/site-context.mjs';

export function getStaticPaths() {
  const { site, template, scheme } = loadContext();
  return site.pages.map((page) => ({
    params: { slug: page.slug === '/' ? undefined : page.slug.slice(1) },
    props: { site, page, template, scheme },
  }));
}

const { site, page, template, scheme } = Astro.props;
---

<Base site={site} page={page} scheme={scheme}>
  {page.blocks.map((block) => (
    <BlockRenderer block={block} site={site} templateId={template.id} />
  ))}
</Base>
```

- [ ] **Step 8: Написать тестовый хелпер сборки**

Файл `tests/helpers/build.mjs`:

Важно: движок пишет предупреждения через `console.warn`, то есть в stderr, а тесты
проверяют их наличие в логе. Поэтому берётся `spawnSync` и оба потока склеиваются —
`execFileSync` вернул бы только stdout, и проверка предупреждений не прошла бы никогда.

```javascript
import { spawnSync } from 'node:child_process';
import { existsSync, readFileSync, rmSync } from 'node:fs';
import { join } from 'node:path';

const ASTRO_BIN = join('node_modules', '.bin', 'astro');

export function buildSite({
  example = 'default',
  template = 't1',
  scheme = 'blue',
  outDir,
  env = {},
} = {}) {
  const target = outDir || join('output', `test-${example}-${template}-${scheme}`);
  rmSync(target, { recursive: true, force: true });

  const examplePublic = join('data', 'examples', example, 'public');
  const result = spawnSync(ASTRO_BIN, ['build'], {
    encoding: 'utf8',
    env: {
      ...process.env,
      SITE_JSON: join('data', 'examples', example, 'site.json'),
      PUBLIC_DIR: existsSync(examplePublic) ? examplePublic : '',
      TEMPLATE: template,
      SCHEME: scheme,
      OUT_DIR: target,
      SITE_URL: 'https://example.com',
      ...env,
    },
  });

  if (result.error) {
    throw new Error(`Cannot run Astro: ${result.error.message}`);
  }
  if (result.status !== 0) {
    throw new Error(`Astro build failed (${result.status}):\n${result.stdout}\n${result.stderr}`);
  }

  return { outDir: target, log: result.stdout + result.stderr };
}

export function readOutput(outDir, file = 'index.html') {
  return readFileSync(join(outDir, file), 'utf8');
}
```

- [ ] **Step 9: Написать падающий тест рендера**

Файл `tests/render.test.mjs`:

```javascript
import { describe, it, expect, beforeAll } from 'vitest';
import { existsSync } from 'node:fs';
import { join } from 'node:path';
import { buildSite, readOutput } from './helpers/build.mjs';

describe('engine build', () => {
  let outDir;

  beforeAll(() => {
    outDir = buildSite({ template: 't1', scheme: 'blue' }).outDir;
  });

  it('writes a home page', () => {
    expect(existsSync(join(outDir, 'index.html'))).toBe(true);
  });

  it('writes every page from the content file', () => {
    expect(existsSync(join(outDir, 'about', 'index.html'))).toBe(true);
  });

  it('renders the hero content', () => {
    expect(readOutput(outDir)).toContain('Find what actually works');
  });

  it('inlines the colour scheme', () => {
    const html = readOutput(outDir);
    expect(html).toContain('--c-primary');
    expect(html).toContain('#2563eb');
  });

  it('sets language and direction', () => {
    expect(readOutput(outDir)).toContain('lang="en"');
    expect(readOutput(outDir)).toContain('dir="ltr"');
  });

  it('copies files from the example public folder', () => {
    expect(existsSync(join(outDir, 'images', 'logo.svg'))).toBe(true);
  });

  it('ships no JavaScript bundles', () => {
    expect(readOutput(outDir)).not.toMatch(/<script[^>]*\ssrc=/i);
  });

  it('survives a broken content file', () => {
    const broken = buildSite({ example: 'broken', outDir: join('output', 'test-broken') });
    expect(existsSync(join(broken.outDir, 'sloppy', 'index.html'))).toBe(true);
    expect(broken.log).toContain('carousel');
  });
});
```

- [ ] **Step 10: Запустить тест и убедиться, что он падает**

Run: `npx vitest run tests/render.test.mjs`
Expected: FAIL — сборки ещё нет либо она падает, если какой-то из файлов задачи не создан.

- [ ] **Step 11: Собрать сайт руками и посмотреть глазами**

```bash
SITE_JSON=data/examples/default/site.json \
PUBLIC_DIR=data/examples/default/public \
TEMPLATE=t1 SCHEME=blue OUT_DIR=output/manual-check \
SITE_URL=https://example.com \
npm run build:site
```

Затем: `open output/manual-check/index.html`
Expected: страница открывается, виден заголовок и футер, фон светло-серый у hero. Блоки richtext/cards/columns/faq отсутствуют — t1 их пока не поддерживает, в логе сборки на каждый есть строка «шаблон не поддерживает блок».

- [ ] **Step 12: Прогнать весь набор**

Run: `npm test`
Expected: PASS.

- [ ] **Step 13: Коммит**

```bash
git add src data/examples tests/helpers tests/render.test.mjs
git commit -m "$(cat <<'MSG'
Движок: контекст из окружения, макет, маршрут и подстановка блоков

BlockRenderer собирает путь к компоненту из id шаблона и типа блока через
import.meta.glob, поэтому ни один шаблон не упомянут в коде движка по имени.
Стили и цветовая схема инлайнятся в head — у собранного сайта ноль внешних
запросов и ноль JS.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
)"
git push
```

---

### Task 6: Шаблон t1 целиком

**Files:**
- Create: `templates/_shared/props.mjs`, `templates/t1/blocks/richtext.astro`, `templates/t1/blocks/cards.astro`, `templates/t1/blocks/columns.astro`, `templates/t1/blocks/faq.astro`
- Modify: `templates/t1/manifest.json`, `data/examples/broken/site.json`, `tests/render.test.mjs`
- Test: `tests/props.test.mjs`

**Interfaces:**
- Consumes: контракт блока из задачи 5 (пропсы блока + `site`)
- Produces:
  - `asList(value) → any[]` и `asText(value, fallback?) → string` из `templates/_shared/props.mjs` — общий хелпер шаблонов, не часть движка;
  - шаблон `t1` поддерживает все шесть типов блоков.

**Почему нужен хелпер.** Нормализатор приводит в порядок структуру блока, но не заглядывает внутрь `props` — он не знает, какие поля нужны конкретному блоку. Значит `props.items` может оказаться строкой, и `.map()` по ней уронит сборку, нарушив главное обещание спека. Каждый компонент прогоняет входные коллекции через `asList`.

- [ ] **Step 1: Написать падающий тест хелпера**

Файл `tests/props.test.mjs`:

```javascript
import { describe, it, expect } from 'vitest';
import { asList, asText } from '../templates/_shared/props.mjs';

describe('block prop helpers', () => {
  it('keeps arrays as they are', () => {
    expect(asList([1, 2])).toEqual([1, 2]);
  });

  it('wraps a single value', () => {
    expect(asList('one')).toEqual(['one']);
  });

  it('turns empty values into an empty list', () => {
    expect(asList(undefined)).toEqual([]);
    expect(asList(null)).toEqual([]);
    expect(asList('')).toEqual([]);
  });

  it('drops empty entries', () => {
    expect(asList(['a', null, '', 'b'])).toEqual(['a', 'b']);
  });

  it('returns the fallback for non-strings', () => {
    expect(asText(42, 'fallback')).toBe('fallback');
    expect(asText('  ', 'fallback')).toBe('fallback');
    expect(asText('text', 'fallback')).toBe('text');
  });
});
```

- [ ] **Step 2: Запустить тест и убедиться, что он падает**

Run: `npx vitest run tests/props.test.mjs`
Expected: FAIL — модуль `../templates/_shared/props.mjs` не найден.

- [ ] **Step 3: Написать хелпер**

Файл `templates/_shared/props.mjs`. Папка начинается с подчёркивания и не содержит `manifest.json`, поэтому реестр шаблонов её не видит.

```javascript
export function asList(value) {
  const list = Array.isArray(value) ? value : [value];
  return list.filter((item) => item !== undefined && item !== null && item !== '');
}

export function asText(value, fallback = '') {
  return typeof value === 'string' && value.trim() !== '' ? value : fallback;
}

export function asRecords(value) {
  return asList(value).filter((item) => typeof item === 'object' && !Array.isArray(item));
}
```

**Правило для всех блоков, которые что-то перечисляют.** Коллекция сначала приводится
к окончательному виду (текст через `asText`, пустые записи отброшены), и только потом
решается, рендерить ли секцию: если показывать нечего, блок не выводит ничего. Заголовок
без содержимого секции не оправдывает. `heading` проходит через `asText` наравне с
остальным текстом — иначе объект в этом поле попал бы в HTML как `[object Object]`.

- [ ] **Step 4: Запустить тест хелпера**

Run: `npx vitest run tests/props.test.mjs`
Expected: PASS, 5 тестов.

- [ ] **Step 5: Написать блок richtext**

Файл `templates/t1/blocks/richtext.astro`:

```astro
---
import { asList, asText } from '../../_shared/props.mjs';

const { heading, paragraphs } = Astro.props;
const title = asText(heading);
const items = asList(paragraphs)
  .map((text) => asText(text))
  .filter((text) => text !== '');
---

{items.length > 0 && (
  <section class="richtext section">
    <div class="container richtext__inner">
      {title && <h2>{title}</h2>}
      {items.map((text) => <p>{text}</p>)}
    </div>
  </section>
)}
<style>
  .richtext__inner {
    display: grid;
    gap: 1.1rem;
    max-width: 44rem;
  }
</style>
```

- [ ] **Step 6: Написать блок cards**

Файл `templates/t1/blocks/cards.astro`:

```astro
---
import { asRecords, asText } from '../../_shared/props.mjs';

const { heading, items } = Astro.props;
const title = asText(heading);
const cards = asRecords(items)
  .map((item) => ({ title: asText(item.title), text: asText(item.text) }))
  .filter((card) => card.title !== '' || card.text !== '');
---

{cards.length > 0 && (
  <section class="cards section section--surface">
    <div class="container">
      {title && <h2 class="cards__heading">{title}</h2>}
      <div class="cards__grid">
        {cards.map((card) => (
          <article class="card">
            <h3>{card.title}</h3>
            <p>{card.text}</p>
          </article>
        ))}
      </div>
    </div>
  </section>
)}
<style>
  .cards__heading {
    margin-bottom: 2rem;
    text-align: center;
  }

  .cards__grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(16rem, 1fr));
    gap: 1.25rem;
  }

  .card {
    padding: 1.75rem;
    border: 1px solid var(--c-border);
    border-radius: var(--radius);
    background: var(--c-bg);
  }

  .card h3 {
    margin-bottom: 0.5rem;
  }

  .card p {
    color: var(--c-muted);
  }
</style>
```

- [ ] **Step 7: Написать блок columns**

Файл `templates/t1/blocks/columns.astro`:

```astro
---
import { asRecords, asList, asText } from '../../_shared/props.mjs';

const { heading, items } = Astro.props;
const title = asText(heading);
const columns = asRecords(items)
  .map((item) => ({
    title: asText(item.title),
    links: asList(item.links)
      .map((link) => asText(link))
      .filter((link) => link !== ''),
  }))
  .filter((column) => column.title !== '' || column.links.length > 0);
---

{columns.length > 0 && (
  <section class="columns section">
    <div class="container">
      {title && <h2 class="columns__heading">{title}</h2>}
      <div class="columns__grid">
        {columns.map((column) => (
          <div class="columns__item">
            <h3>{column.title}</h3>
            {column.links.length > 0 && (
              <ul>
                {column.links.map((link) => (
                  <li>{link}</li>
                ))}
              </ul>
            )}
          </div>
        ))}
      </div>
    </div>
  </section>
)}
<style>
  .columns__heading {
    margin-bottom: 2rem;
  }

  .columns__grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(13rem, 1fr));
    gap: 2rem;
  }

  .columns__item h3 {
    margin-bottom: 0.75rem;
    padding-bottom: 0.5rem;
    border-bottom: 2px solid var(--c-primary);
  }

  .columns__item ul {
    list-style: none;
    padding: 0;
    display: grid;
    gap: 0.4rem;
    color: var(--c-muted);
  }
</style>
```

- [ ] **Step 8: Написать блок faq**

Файл `templates/t1/blocks/faq.astro`:

```astro
---
import { asRecords, asText } from '../../_shared/props.mjs';

const { heading, items } = Astro.props;
const title = asText(heading);
const questions = asRecords(items)
  .map((item) => ({ q: asText(item.q), a: asText(item.a) }))
  .filter((entry) => entry.q !== '' || entry.a !== '');
---

{questions.length > 0 && (
  <section class="faq section section--surface">
    <div class="container faq__inner">
      {title && <h2>{title}</h2>}
      <dl class="faq__list">
        {questions.map((item) => (
          <div class="faq__item">
            <dt>{item.q}</dt>
            <dd>{item.a}</dd>
          </div>
        ))}
      </dl>
    </div>
  </section>
)}
<style>
  .faq__inner {
    max-width: 46rem;
  }

  .faq__list {
    margin-top: 1.75rem;
    display: grid;
    gap: 1.25rem;
  }

  .faq__item {
    padding-bottom: 1.25rem;
    border-bottom: 1px solid var(--c-border);
  }

  .faq__item dt {
    font-weight: 650;
    margin-bottom: 0.35rem;
  }

  .faq__item dd {
    margin: 0;
    color: var(--c-muted);
  }
</style>
```

- [ ] **Step 9: Расширить манифест**

Заменить содержимое `templates/t1/manifest.json`:

```json
{
  "id": "t1",
  "name": "Классический",
  "description": "Широкий hero по центру, карточки сеткой три в ряд",
  "blocks": ["hero", "richtext", "cards", "columns", "faq", "footer"],
  "defaultScheme": "blue"
}
```

- [ ] **Step 10: Добавить в битый пример блок с испорченными props**

Заменить содержимое `data/examples/broken/site.json`. Здесь `items` — строка вместо массива: без `asList` это уронило бы сборку.

```json
{
  "brand": { "name": "Broken Example" },
  "pages": [
    {
      "slug": "sloppy//",
      "blocks": [
        { "type": "hero" },
        { "props": { "title": "no type at all" } },
        "just a string",
        { "type": "carousel", "props": { "items": [] } },
        { "type": "cards", "props": { "items": "not an array" } },
        { "type": "faq", "props": { "items": [null, { "q": "only a question" }] } },
        { "type": "richtext", "props": { "paragraphs": "a single string" } }
      ]
    }
  ]
}
```

- [ ] **Step 11: Дописать тесты рендера**

Добавить в `tests/render.test.mjs` внутрь `describe('engine build', …)`, после теста `ships no JavaScript bundles`:

```javascript
  it('renders every block type the template declares', () => {
    const html = readOutput(outDir);
    expect(html).toContain('Why this guide exists');
    expect(html).toContain('What we check');
    expect(html).toContain('Browse by topic');
    expect(html).toContain('Questions we get a lot');
    expect(html).toContain('Материалы носят информационный характер');
  });
```

И заменить тест `survives a broken content file` на:

```javascript
  it('survives a broken content file', () => {
    const broken = buildSite({ example: 'broken', outDir: join('output', 'test-broken') });
    const html = readOutput(broken.outDir, join('sloppy', 'index.html'));
    expect(html).toContain('a single string');
    expect(html).toContain('only a question');
    expect(broken.log).toContain('carousel');
  });
```

- [ ] **Step 12: Запустить тесты рендера**

Run: `npx vitest run tests/render.test.mjs`
Expected: PASS, 9 тестов.

- [ ] **Step 13: Посмотреть результат глазами**

```bash
SITE_JSON=data/examples/default/site.json \
PUBLIC_DIR=data/examples/default/public \
TEMPLATE=t1 SCHEME=blue OUT_DIR=output/manual-check \
npm run build:site && open output/manual-check/index.html
```

Expected: страница целиком — hero, текстовый блок, три карточки, три колонки ссылок, FAQ, футер. При сужении окна до 375px вёрстка не разъезжается.

- [ ] **Step 14: Прогнать весь набор**

Run: `npm test`
Expected: PASS.

- [ ] **Step 15: Коммит**

```bash
git add templates data/examples/broken tests/props.test.mjs tests/render.test.mjs
git commit -m "$(cat <<'MSG'
Шаблон t1 целиком: шесть блоков

Компоненты прогоняют коллекции через asList: нормализатор чинит структуру
блока, но не заглядывает внутрь props, поэтому строка вместо массива должна
обрабатываться самим блоком, а не ронять сборку.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
)"
git push
```

---

### Task 7: Шаблон t2 «Разделённый»

Тот же контент, другая вёрстка: всё выравнено по левому краю, hero разбит на две колонки, карточки нумерованные.

**Files:**
- Create: `templates/t2/manifest.json`, `templates/t2/blocks/{hero,richtext,cards,columns,faq,footer}.astro`
- Test: `tests/render.test.mjs` (дополняется)

**Interfaces:**
- Consumes: контракт блока из задачи 5, `asList`/`asText` из задачи 6
- Produces: шаблон `t2`, принимающий тот же контент, что и `t1`

- [ ] **Step 1: Создать манифест**

Файл `templates/t2/manifest.json`:

```json
{
  "id": "t2",
  "name": "Разделённый",
  "description": "Hero в две колонки, нумерованные карточки, плотная сетка",
  "blocks": ["hero", "richtext", "cards", "columns", "faq", "footer"],
  "defaultScheme": "green"
}
```

- [ ] **Step 2: Написать блок hero**

Файл `templates/t2/blocks/hero.astro`:

```astro
---
import { asText } from '../../_shared/props.mjs';

const { title, subtitle, cta, image, site } = Astro.props;
const heading = asText(title, site?.brand?.name || 'Заголовок');
const subtitleText = asText(subtitle);
const buttonLabel = asText(cta, 'Подробнее');
const imageSrc = asText(image);
---

<section class="hero">
  <div class="container hero__grid">
    <div class="hero__text">
      {site?.brand?.name && <p class="eyebrow">{site.brand.name}</p>}
      <h1>{heading}</h1>
      {subtitleText && <p class="hero__subtitle">{subtitleText}</p>}
      {site?.partnerUrl && <a class="button" href={site.partnerUrl}>{buttonLabel}</a>}
    </div>
    <div class="hero__aside">
      {imageSrc ? <img src={imageSrc} alt="" /> : <div class="hero__panel" />}
    </div>
  </div>
</section>
<style>
  .hero {
    padding-block: var(--space);
    border-bottom: 1px solid var(--c-border);
  }

  .hero__grid {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    align-items: center;
    gap: 3rem;
  }

  .hero__text {
    display: grid;
    gap: 1.15rem;
    justify-items: start;
  }

  .hero__subtitle {
    color: var(--c-muted);
    font-size: 1.15rem;
    max-width: 34rem;
  }

  .hero__panel {
    aspect-ratio: 4 / 3;
    border-radius: var(--radius);
    background: linear-gradient(140deg, var(--c-primary), var(--c-accent));
  }

  .hero__aside img {
    border-radius: var(--radius);
  }

  @media (max-width: 760px) {
    .hero__grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
  }
</style>
```

- [ ] **Step 3: Написать блок richtext**

Файл `templates/t2/blocks/richtext.astro`:

```astro
---
import { asList, asText } from '../../_shared/props.mjs';

const { heading, paragraphs } = Astro.props;
const title = asText(heading);
const items = asList(paragraphs)
  .map((text) => asText(text))
  .filter((text) => text !== '');
---

{items.length > 0 && (
  <section class="richtext section">
    <div class="container richtext__grid">
      {title && <h2 class="richtext__heading">{title}</h2>}
      <div class="richtext__body">
        {items.map((text) => <p>{text}</p>)}
      </div>
    </div>
  </section>
)}

<style>
  .richtext__grid {
    display: grid;
    grid-template-columns: 1fr 1.6fr;
    gap: 2.5rem;
    align-items: start;
  }

  .richtext__heading {
    position: sticky;
    top: 2rem;
  }

  .richtext__body {
    display: grid;
    gap: 1rem;
  }

  @media (max-width: 760px) {
    .richtext__grid {
      grid-template-columns: 1fr;
      gap: 1.25rem;
    }

    .richtext__heading {
      position: static;
    }
  }
</style>
```

- [ ] **Step 4: Написать блок cards**

Файл `templates/t2/blocks/cards.astro`:

```astro
---
import { asRecords, asText } from '../../_shared/props.mjs';

const { heading, items } = Astro.props;
const title = asText(heading);
const cards = asRecords(items)
  .map((item) => ({ title: asText(item.title), text: asText(item.text) }))
  .filter((card) => card.title !== '' || card.text !== '');
---

{cards.length > 0 && (
  <section class="cards section">
    <div class="container">
      {title && <h2>{title}</h2>}
      <div class="cards__grid">
        {cards.map((card, index) => (
          <article class="card">
            <span class="card__number">{String(index + 1).padStart(2, '0')}</span>
            <div>
              <h3>{card.title}</h3>
              <p>{card.text}</p>
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>
)}

<style>
  .cards__grid {
    margin-top: 2rem;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(22rem, 1fr));
    gap: 1.5rem 2.5rem;
  }

  .card {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 1.1rem;
    padding-top: 1.25rem;
    border-top: 2px solid var(--c-primary);
  }

  .card__number {
    color: var(--c-primary);
    font-size: 1.6rem;
    font-weight: 700;
    line-height: 1;
  }

  .card h3 {
    margin-bottom: 0.4rem;
  }

  .card p {
    color: var(--c-muted);
  }
</style>
```

- [ ] **Step 5: Написать блок columns**

Файл `templates/t2/blocks/columns.astro`:

```astro
---
import { asRecords, asList, asText } from '../../_shared/props.mjs';

const { heading, items } = Astro.props;
const title = asText(heading);
const columns = asRecords(items)
  .map((item) => ({
    title: asText(item.title),
    links: asList(item.links)
      .map((link) => asText(link))
      .filter((link) => link !== ''),
  }))
  .filter((column) => column.title !== '' || column.links.length > 0);
---

{columns.length > 0 && (
  <section class="columns section section--surface">
    <div class="container">
      {title && <h2>{title}</h2>}
      <div class="columns__rows">
        {columns.map((column) => (
          <div class="columns__row">
            <h3>{column.title}</h3>
            {column.links.length > 0 && (
              <ul>
                {column.links.map((link) => (
                  <li>{link}</li>
                ))}
              </ul>
            )}
          </div>
        ))}
      </div>
    </div>
  </section>
)}

<style>
  .columns__rows {
    margin-top: 1.75rem;
    display: grid;
    gap: 0.5rem;
  }

  .columns__row {
    display: grid;
    grid-template-columns: 14rem 1fr;
    gap: 1.5rem;
    padding-block: 1.1rem;
    border-bottom: 1px solid var(--c-border);
  }

  .columns__row ul {
    list-style: none;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem 1.5rem;
    color: var(--c-muted);
  }

  @media (max-width: 760px) {
    .columns__row {
      grid-template-columns: 1fr;
      gap: 0.5rem;
    }
  }
</style>
```

- [ ] **Step 6: Написать блок faq**

Файл `templates/t2/blocks/faq.astro`:

```astro
---
import { asRecords, asText } from '../../_shared/props.mjs';

const { heading, items } = Astro.props;
const title = asText(heading);
const questions = asRecords(items)
  .map((item) => ({ q: asText(item.q), a: asText(item.a) }))
  .filter((entry) => entry.q !== '' || entry.a !== '');
---

{questions.length > 0 && (
  <section class="faq section">
    <div class="container">
      {title && <h2>{title}</h2>}
      <div class="faq__grid">
        {questions.map((item) => (
          <div class="faq__item">
            <p class="faq__q">{item.q}</p>
            <p class="faq__a">{item.a}</p>
          </div>
        ))}
      </div>
    </div>
  </section>
)}

<style>
  .faq__grid {
    margin-top: 2rem;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(20rem, 1fr));
    gap: 1.75rem 2.5rem;
  }

  .faq__q {
    font-weight: 650;
    margin-bottom: 0.4rem;
  }

  .faq__a {
    color: var(--c-muted);
  }
</style>
```

- [ ] **Step 7: Написать блок footer**

Файл `templates/t2/blocks/footer.astro`:

```astro
---
const { site } = Astro.props;
const year = new Date().getFullYear();
const disclaimer =
  'Информация предоставлена в ознакомительных целях и может быть изменена без уведомления.';
---

<footer class="footer">
  <div class="container footer__grid">
    <div>
      <p class="footer__brand">{site?.brand?.name}</p>
      <p class="footer__copy">© {year} {site?.domain}</p>
    </div>
    <p class="footer__disclaimer">{disclaimer}</p>
  </div>
</footer>

<style>
  .footer {
    padding-block: 2.5rem;
    background: var(--c-fg);
    color: var(--c-bg);
  }

  .footer__grid {
    display: grid;
    grid-template-columns: 1fr 1.5fr;
    gap: 2rem;
    align-items: center;
  }

  .footer__brand {
    font-weight: 700;
  }

  .footer__copy,
  .footer__disclaimer {
    font-size: 0.9rem;
    opacity: 0.75;
  }

  @media (max-width: 760px) {
    .footer__grid {
      grid-template-columns: 1fr;
      gap: 1rem;
    }
  }
</style>
```

- [ ] **Step 8: Дописать тест взаимозаменяемости**

Добавить в конец `tests/render.test.mjs`, отдельным блоком после `describe('engine build', …)`:

```javascript
describe('templates are interchangeable', () => {
  it('renders the same content with t2', () => {
    const { outDir } = buildSite({ template: 't2', scheme: 'green' });
    const html = readOutput(outDir);
    expect(html).toContain('Find what actually works');
    expect(html).toContain('Questions we get a lot');
    expect(html).toContain('#10794a');
    expect(html).not.toMatch(/<script[^>]*\ssrc=/i);
  });
});
```

- [ ] **Step 9: Запустить тесты**

Run: `npx vitest run tests/render.test.mjs`
Expected: PASS.

- [ ] **Step 10: Посмотреть глазами**

```bash
SITE_JSON=data/examples/default/site.json \
PUBLIC_DIR=data/examples/default/public \
TEMPLATE=t2 SCHEME=green OUT_DIR=output/manual-t2 \
npm run build:site && open output/manual-t2/index.html
```

Expected: hero в две колонки с градиентной плашкой справа, карточки с номерами 01/02/03, тёмный футер, зелёные акценты. Явно не похоже на t1.

- [ ] **Step 11: Прогнать весь набор**

Run: `npm test`
Expected: PASS.

- [ ] **Step 12: Коммит**

```bash
git add templates/t2 tests/render.test.mjs
git commit -m "$(cat <<'MSG'
Шаблон t2 «Разделённый»

Тот же контент-JSON, другая вёрстка: hero в две колонки, нумерованные
карточки, строчные колонки ссылок, тёмный футер.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
)"
git push
```

---

### Task 8: Шаблон t3 «Компактный»

Третья вёрстка: узкая колонка, крупная типографика, FAQ раскрывающимися блоками на `<details>` — интерактив без единой строчки JavaScript.

Все блоки t3 используют общий класс `.shell` из `styles/base.css` (узкая колонка 44rem). Он объявлен один раз в базовых стилях, а не в каждом блоке: scoped-стили Astro изолируют селекторы компонента, но глобальные правила из инлайненного `base.css` к элементам применяются, поэтому повторять определение в шести файлах не нужно.

**Files:**
- Create: `templates/t3/manifest.json`, `templates/t3/blocks/{hero,richtext,cards,columns,faq,footer}.astro`
- Modify: `tests/render.test.mjs`

**Interfaces:**
- Consumes: контракт блока из задачи 5, `asList`/`asText` из задачи 6
- Produces: шаблон `t3`; после этой задачи все три шаблона взаимозаменяемы и различимы

- [ ] **Step 1: Создать манифест**

Файл `templates/t3/manifest.json`:

```json
{
  "id": "t3",
  "name": "Компактный",
  "description": "Узкая колонка, крупная типографика, FAQ раскрывающимися блоками",
  "blocks": ["hero", "richtext", "cards", "columns", "faq", "footer"],
  "defaultScheme": "dark"
}
```

- [ ] **Step 2: Написать блок hero**

Файл `templates/t3/blocks/hero.astro`:

```astro
---
import { asText } from '../../_shared/props.mjs';

const { title, subtitle, cta, site } = Astro.props;
const heading = asText(title, asText(site?.brand?.name, 'Заголовок'));
const lead = asText(subtitle);
const buttonLabel = asText(cta, 'Подробнее');
---

<section class="hero">
  <div class="shell">
    <div class="hero__badge">{site?.brand?.name}</div>
    <h1>{heading}</h1>
    {lead && <p class="hero__subtitle">{lead}</p>}
    {site?.partnerUrl && <a class="button" href={site.partnerUrl}>{buttonLabel}</a>}
  </div>
</section>

<style>
  .hero {
    padding-block: clamp(3rem, 9vw, 6rem);
  }

  .hero__badge {
    display: inline-block;
    margin-bottom: 1.5rem;
    padding: 0.35rem 0.85rem;
    border: 1px solid var(--c-border);
    border-radius: 999px;
    color: var(--c-muted);
    font-size: 0.8rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .hero__subtitle {
    margin-block: 1.25rem 1.75rem;
    color: var(--c-muted);
    font-size: 1.25rem;
  }
</style>
```

- [ ] **Step 3: Написать блок richtext**

Файл `templates/t3/blocks/richtext.astro`:

```astro
---
import { asList, asText } from '../../_shared/props.mjs';

const { heading, paragraphs } = Astro.props;
const title = asText(heading);
const items = asList(paragraphs)
  .map((text) => asText(text))
  .filter((text) => text !== '');
---

{items.length > 0 && (
  <section class="richtext">
    <div class="shell richtext__inner">
      {title && <h2>{title}</h2>}
      {items.map((text) => <p>{text}</p>)}
    </div>
  </section>
)}

<style>
  .richtext {
    padding-block: 2.5rem;
  }

  .richtext__inner {
    display: grid;
    gap: 1rem;
    padding-inline-start: 1.5rem;
    border-inline-start: 3px solid var(--c-primary);
  }
</style>
```

- [ ] **Step 4: Написать блок cards**

Файл `templates/t3/blocks/cards.astro`:

```astro
---
import { asRecords, asText } from '../../_shared/props.mjs';

const { heading, items } = Astro.props;
const title = asText(heading);
const cards = asRecords(items)
  .map((item) => ({ title: asText(item.title), text: asText(item.text) }))
  .filter((card) => card.title !== '' || card.text !== '');
---

{cards.length > 0 && (
  <section class="cards">
    <div class="shell">
      {title && <h2>{title}</h2>}
      <ol class="cards__list">
        {cards.map((card) => (
          <li class="cards__item">
            <h3>{card.title}</h3>
            <p>{card.text}</p>
          </li>
        ))}
      </ol>
    </div>
  </section>
)}

<style>
  .cards {
    padding-block: 2.5rem;
  }

  .cards__list {
    margin-top: 1.5rem;
    padding: 0;
    list-style: none;
    counter-reset: card;
    display: grid;
    gap: 1rem;
  }

  .cards__item {
    counter-increment: card;
    padding: 1.25rem 1.25rem 1.25rem 3.5rem;
    position: relative;
    border-radius: var(--radius-sm);
    background: var(--c-surface);
  }

  .cards__item::before {
    content: counter(card);
    position: absolute;
    inset-inline-start: 1.25rem;
    top: 1.25rem;
    width: 1.6rem;
    height: 1.6rem;
    display: grid;
    place-items: center;
    border-radius: 50%;
    background: var(--c-primary);
    color: var(--c-primary-fg);
    font-size: 0.85rem;
    font-weight: 700;
  }

  .cards__item p {
    color: var(--c-muted);
  }
</style>
```

- [ ] **Step 5: Написать блок columns**

Файл `templates/t3/blocks/columns.astro`:

```astro
---
import { asRecords, asList, asText } from '../../_shared/props.mjs';

const { heading, items } = Astro.props;
const title = asText(heading);
const columns = asRecords(items)
  .map((item) => ({
    title: asText(item.title),
    links: asList(item.links)
      .map((link) => asText(link))
      .filter((link) => link !== ''),
  }))
  .filter((column) => column.title !== '' || column.links.length > 0);
---

{columns.length > 0 && (
  <section class="columns">
    <div class="shell">
      {title && <h2>{title}</h2>}
      <div class="columns__groups">
        {columns.map((column) => (
          <div class="columns__group">
            <h3>{column.title}</h3>
            {column.links.length > 0 && (
              <div class="columns__tags">
                {column.links.map((link) => (
                  <span class="tag">{link}</span>
                ))}
              </div>
            )}
          </div>
        ))}
      </div>
    </div>
  </section>
)}

<style>
  .columns {
    padding-block: 2.5rem;
  }

  .columns__groups {
    margin-top: 1.5rem;
    display: grid;
    gap: 1.5rem;
  }

  .columns__group h3 {
    margin-bottom: 0.6rem;
    font-size: 0.95rem;
    color: var(--c-muted);
  }

  .columns__tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .tag {
    padding: 0.35rem 0.8rem;
    border: 1px solid var(--c-border);
    border-radius: 999px;
    font-size: 0.9rem;
  }
</style>
```

- [ ] **Step 6: Написать блок faq**

Файл `templates/t3/blocks/faq.astro`. Первый вопрос открыт по умолчанию, остальные раскрываются нативно — `<details>` не требует JavaScript.

```astro
---
import { asRecords, asText } from '../../_shared/props.mjs';

const { heading, items } = Astro.props;
const title = asText(heading);
const questions = asRecords(items)
  .map((item) => ({ q: asText(item.q), a: asText(item.a) }))
  .filter((entry) => entry.q !== '' || entry.a !== '');
---

{questions.length > 0 && (
  <section class="faq">
    <div class="shell">
      {title && <h2>{title}</h2>}
      <div class="faq__list">
        {questions.map((item, index) => (
          <details class="faq__item" open={index === 0}>
            <summary>{item.q}</summary>
            <p>{item.a}</p>
          </details>
        ))}
      </div>
    </div>
  </section>
)}

<style>
  .faq {
    padding-block: 2.5rem;
  }

  .faq__list {
    margin-top: 1.5rem;
    display: grid;
    gap: 0.5rem;
  }

  .faq__item {
    padding: 1rem 1.25rem;
    border: 1px solid var(--c-border);
    border-radius: var(--radius-sm);
  }

  .faq__item summary {
    cursor: pointer;
    font-weight: 650;
  }

  .faq__item p {
    margin-top: 0.75rem;
    color: var(--c-muted);
  }
</style>
```

- [ ] **Step 7: Написать блок footer**

Файл `templates/t3/blocks/footer.astro`:

```astro
---
const { site } = Astro.props;
const year = new Date().getFullYear();
const disclaimer = 'Данный обзор носит информационный характер.';
---

<footer class="footer">
  <div class="shell footer__inner">
    <p>{disclaimer}</p>
    <p class="footer__meta">
      {site?.brand?.name} · © {year} {site?.domain}
    </p>
  </div>
</footer>

<style>
  .footer {
    margin-top: 2rem;
    padding-block: 2rem;
    border-top: 1px solid var(--c-border);
  }

  .footer__inner {
    display: grid;
    gap: 0.5rem;
    color: var(--c-muted);
    font-size: 0.9rem;
  }

  .footer__meta {
    font-weight: 600;
  }
</style>
```

- [ ] **Step 8: Дописать тест различимости**

Заменить в `tests/render.test.mjs` блок `describe('templates are interchangeable', …)` целиком на:

```javascript
describe('templates are interchangeable and distinct', () => {
  const rendered = {};

  beforeAll(() => {
    for (const template of ['t1', 't2', 't3']) {
      const { outDir } = buildSite({ template, scheme: 'blue' });
      rendered[template] = readOutput(outDir);
    }
  });

  it('renders the same content in every template', () => {
    for (const html of Object.values(rendered)) {
      expect(html).toContain('Find what actually works');
      expect(html).toContain('Why this guide exists');
      expect(html).toContain('Questions we get a lot');
    }
  });

  it('produces visibly different markup per template', () => {
    const [first, second, third] = Object.values(rendered);
    expect(first).not.toBe(second);
    expect(second).not.toBe(third);
    expect(first).not.toBe(third);
  });

  it('renders the FAQ as native disclosure widgets in t3 only', () => {
    expect(rendered.t3).toContain('<details');
    expect(rendered.t1).not.toContain('<details');
    expect(rendered.t2).not.toContain('<details');
  });

  it('ships no JavaScript in any template', () => {
    for (const html of Object.values(rendered)) {
      expect(html).not.toMatch(/<script[^>]*\ssrc=/i);
    }
  });
});
```

Проверить, что в начале файла импортируется `beforeAll` — строка должна выглядеть так:

```javascript
import { describe, it, expect, beforeAll } from 'vitest';
```

- [ ] **Step 9: Запустить тесты**

Run: `npx vitest run tests/render.test.mjs`
Expected: PASS.

- [ ] **Step 10: Посмотреть глазами**

```bash
SITE_JSON=data/examples/default/site.json \
PUBLIC_DIR=data/examples/default/public \
TEMPLATE=t3 SCHEME=dark OUT_DIR=output/manual-t3 \
npm run build:site && open output/manual-t3/index.html
```

Expected: тёмный фон, узкая колонка, плашка-бейдж в hero, нумерованные строки вместо карточек, FAQ раскрывается кликом без JS. Проверить: клик по вопросу раскрывает ответ.

- [ ] **Step 11: Прогнать весь набор**

Run: `npm test`
Expected: PASS.

- [ ] **Step 12: Коммит**

```bash
git add templates/t3 tests/render.test.mjs
git commit -m "$(cat <<'MSG'
Шаблон t3 «Компактный»

Узкая колонка, крупная типографика, FAQ на нативных details — раскрытие
работает без единой строчки клиентского JS. Тест подтверждает: три шаблона
дают разную разметку на одном и том же контенте.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
)"
git push
```

---

### Task 9: SEO-артефакты

**Files:**
- Create: `src/pages/sitemap.xml.js`, `src/pages/robots.txt.js`
- Test: `tests/seo.test.mjs`

**Interfaces:**
- Consumes: `loadContext()` (задача 5)
- Produces: файлы `sitemap.xml` и `robots.txt` в корне каждого собранного сайта

- [ ] **Step 1: Написать падающий тест**

Файл `tests/seo.test.mjs`:

```javascript
import { describe, it, expect, beforeAll } from 'vitest';
import { buildSite, readOutput } from './helpers/build.mjs';

describe('SEO artefacts', () => {
  let outDir;

  beforeAll(() => {
    outDir = buildSite({
      template: 't1',
      scheme: 'blue',
      outDir: 'output/test-seo',
      env: {},
    }).outDir;
  });

  it('lists every page in the sitemap', () => {
    const xml = readOutput(outDir, 'sitemap.xml');
    expect(xml).toContain('<urlset');
    expect(xml).toContain('https://example.com/');
    expect(xml).toContain('https://example.com/about');
  });

  it('points robots.txt at the sitemap', () => {
    const txt = readOutput(outDir, 'robots.txt');
    expect(txt).toContain('User-agent: *');
    expect(txt).toContain('Allow: /');
    expect(txt).toContain('Sitemap: https://example.com/sitemap.xml');
  });

  it('sets a canonical link on every page', () => {
    expect(readOutput(outDir)).toContain(
      '<link rel="canonical" href="https://example.com/"',
    );
    expect(readOutput(outDir, 'about/index.html')).toContain(
      '<link rel="canonical" href="https://example.com/about"',
    );
  });

  it('writes the geo meta tag only when geo is set', () => {
    expect(readOutput(outDir)).not.toContain('geo.region');

    const withGeo = buildSite({
      template: 't1',
      scheme: 'blue',
      outDir: 'output/test-seo-geo',
      env: { GEO: 'ID' },
    });
    expect(readOutput(withGeo.outDir)).toContain('name="geo.region" content="ID"');
  });
});
```

- [ ] **Step 2: Запустить тест и убедиться, что он падает**

Run: `npx vitest run tests/seo.test.mjs`
Expected: FAIL — файла `sitemap.xml` в сборке нет.

- [ ] **Step 3: Написать sitemap**

Файл `src/pages/sitemap.xml.js`:

```javascript
import { loadContext } from '../lib/site-context.mjs';

export function GET(context) {
  const { site } = loadContext();
  const origin = (context.site?.origin ?? `https://${site.domain}`).replace(/\/$/, '');
  const urls = site.pages
    .map((page) => `  <url><loc>${origin}${page.slug}</loc></url>`)
    .join('\n');

  const xml = `<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
${urls}
</urlset>
`;

  return new Response(xml, {
    headers: { 'Content-Type': 'application/xml; charset=utf-8' },
  });
}
```

- [ ] **Step 4: Написать robots.txt**

Файл `src/pages/robots.txt.js`:

```javascript
import { loadContext } from '../lib/site-context.mjs';

export function GET(context) {
  const { site } = loadContext();
  const origin = (context.site?.origin ?? `https://${site.domain}`).replace(/\/$/, '');
  const body = `User-agent: *
Allow: /

Sitemap: ${origin}/sitemap.xml
`;

  return new Response(body, {
    headers: { 'Content-Type': 'text/plain; charset=utf-8' },
  });
}
```

- [ ] **Step 5: Запустить тесты**

Run: `npx vitest run tests/seo.test.mjs`
Expected: PASS, 4 теста.

- [ ] **Step 6: Прогнать весь набор**

Run: `npm test`
Expected: PASS.

- [ ] **Step 7: Коммит**

```bash
git add src/pages/sitemap.xml.js src/pages/robots.txt.js tests/seo.test.mjs
git commit -m "$(cat <<'MSG'
SEO-артефакты: sitemap, robots, canonical

Оба эндпоинта берут список страниц из того же контекста, что и рендер,
поэтому карта сайта не может разойтись с фактическими страницами.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
)"
git push
```

---

### Task 10: Сервер фабрики — списки и запуск сборки

**Files:**
- Create: `factory/server.mjs`
- Test: `tests/server.test.mjs`

**Interfaces:**
- Consumes: `listTemplates()` (задача 4), `listSchemes()` (задача 3)
- Produces:
  - `createApp() → express.Application` — приложение без прослушивания порта, чтобы тесты поднимали его на случайном порту;
  - `GET /api/templates → { templates: Manifest[] }`;
  - `GET /api/schemes → { schemes: string[] }`;
  - `GET /api/examples → { examples: string[] }`;
  - `POST /api/generate` принимает `{ brand, geo, locale, template, scheme, example, domain, partnerUrl }`, отвечает `{ buildId, domain, outDir }`;
  - `GET /api/builds/:id → { id, status, domain, outDir, lines }`, где `status` ∈ `running | ok | failed`;
  - `GET /api/builds/:id/log` — поток `text/event-stream`: события с данными-строками лога и финальное событие `done` с итоговым статусом.

- [ ] **Step 1: Написать падающий тест**

Файл `tests/server.test.mjs`:

```javascript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { existsSync, rmSync } from 'node:fs';
import { join } from 'node:path';
import { createApp } from '../factory/server.mjs';

let server;
let base;

beforeAll(async () => {
  server = createApp().listen(0);
  await new Promise((resolve) => server.once('listening', resolve));
  base = `http://127.0.0.1:${server.address().port}`;
});

afterAll(() => {
  server?.close();
});

async function readUntilDone(buildId) {
  const response = await fetch(`${base}/api/builds/${buildId}/log`);
  const reader = response.body.getReader();
  const decoder = new TextDecoder();
  let text = '';
  while (!text.includes('event: done')) {
    const { value, done } = await reader.read();
    if (done) break;
    text += decoder.decode(value, { stream: true });
  }
  await reader.cancel();
  return text;
}

describe('factory API', () => {
  it('lists templates read from disk', async () => {
    const data = await fetch(`${base}/api/templates`).then((r) => r.json());
    expect(data.templates.map((t) => t.id)).toEqual(['t1', 't2', 't3']);
    expect(data.templates[0].name).toBeTruthy();
  });

  it('lists colour schemes read from disk', async () => {
    const data = await fetch(`${base}/api/schemes`).then((r) => r.json());
    expect(data.schemes).toEqual(['blue', 'dark', 'green']);
  });

  it('lists example folders that actually hold content', async () => {
    const data = await fetch(`${base}/api/examples`).then((r) => r.json());
    expect(data.examples).toContain('default');
    expect(data.examples).toContain('broken');
  });

  it('builds a site and reports success', async () => {
    rmSync(join('output', 'api-test.com'), { recursive: true, force: true });

    const start = await fetch(`${base}/api/generate`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        brand: 'API Test',
        template: 't2',
        scheme: 'green',
        example: 'default',
        domain: 'api-test.com',
        partnerUrl: 'https://partner.example/go',
      }),
    }).then((r) => r.json());

    expect(start.buildId).toBeTruthy();
    expect(start.domain).toBe('api-test.com');

    const log = await readUntilDone(start.buildId);
    expect(log).toContain('event: done');

    const status = await fetch(`${base}/api/builds/${start.buildId}`).then((r) => r.json());
    expect(status.status).toBe('ok');
    expect(existsSync(join('output', 'api-test.com', 'index.html'))).toBe(true);
  });

  it('falls back to the example name when no domain is given', async () => {
    const start = await fetch(`${base}/api/generate`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ example: 'default', template: 't1', scheme: 'blue' }),
    }).then((r) => r.json());

    expect(start.domain).toBe('default');
    await readUntilDone(start.buildId);
  });

  it('refuses an example that does not exist', async () => {
    const response = await fetch(`${base}/api/generate`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ example: '../../etc' }),
    });
    expect(response.status).toBe(400);
  });

  it('answers 404 for an unknown build', async () => {
    const response = await fetch(`${base}/api/builds/nope`);
    expect(response.status).toBe(404);
  });
});
```

- [ ] **Step 2: Запустить тест и убедиться, что он падает**

Run: `npx vitest run tests/server.test.mjs`
Expected: FAIL — модуль `../factory/server.mjs` не найден.

- [ ] **Step 3: Написать сервер**

Файл `factory/server.mjs`. Имена папок приходят из браузера, поэтому каждое проходит через `safeName` — без этого `example: '../../etc'` увёл бы сборку за пределы проекта.

```javascript
import { spawn } from 'node:child_process';
import { randomUUID } from 'node:crypto';
import { existsSync, readdirSync } from 'node:fs';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';
import express from 'express';
import { listTemplates } from '../src/lib/templates.mjs';
import { listSchemes } from '../src/lib/schemes.mjs';

const HERE = dirname(fileURLToPath(import.meta.url));
const ROOT = join(HERE, '..');
const EXAMPLES_DIR = join(ROOT, 'data', 'examples');
const OUTPUT_DIR = join(ROOT, 'output');
const ASTRO_BIN = join(ROOT, 'node_modules', '.bin', 'astro');
const PORT = Number(process.env.PORT || 3002);

const builds = new Map();

function safeName(value, fallback = '') {
  const cleaned = String(value ?? '')
    .trim()
    .toLowerCase()
    .replace(/[^a-z0-9._-]+/g, '-')
    .replace(/\.{2,}/g, '.')
    .replace(/^[-.]+|[-.]+$/g, '');
  return cleaned || fallback;
}

function listExamples() {
  if (!existsSync(EXAMPLES_DIR)) return [];
  return readdirSync(EXAMPLES_DIR)
    .filter((name) => existsSync(join(EXAMPLES_DIR, name, 'site.json')))
    .sort();
}

function pushLine(build, line) {
  build.lines.push(line);
  for (const listener of build.listeners) listener.send(line);
}

function finishBuild(build, status) {
  build.status = status;
  for (const listener of build.listeners) listener.finish(status);
  build.listeners.clear();
}

function startBuild(options) {
  const build = {
    id: randomUUID(),
    status: 'running',
    domain: options.domain,
    outDir: options.outDir,
    lines: [],
    listeners: new Set(),
  };
  builds.set(build.id, build);

  const child = spawn(ASTRO_BIN, ['build'], {
    cwd: ROOT,
    env: { ...process.env, ...options.env },
  });

  let buffer = '';
  const consume = (chunk) => {
    buffer += chunk.toString();
    const parts = buffer.split('\n');
    buffer = parts.pop() ?? '';
    for (const part of parts) pushLine(build, part);
  };

  child.stdout.on('data', consume);
  child.stderr.on('data', consume);

  child.on('error', (error) => {
    pushLine(build, `Не удалось запустить сборку: ${error.message}`);
    finishBuild(build, 'failed');
  });

  child.on('close', (code) => {
    if (buffer !== '') pushLine(build, buffer);
    pushLine(build, code === 0 ? 'Готово' : `Сборка завершилась с кодом ${code}`);
    finishBuild(build, code === 0 ? 'ok' : 'failed');
  });

  return build;
}

export function createApp() {
  const app = express();
  app.use(express.json());

  app.get('/api/templates', (_req, res) => {
    res.json({ templates: listTemplates(ROOT) });
  });

  app.get('/api/schemes', (_req, res) => {
    res.json({ schemes: listSchemes(ROOT) });
  });

  app.get('/api/examples', (_req, res) => {
    res.json({ examples: listExamples() });
  });

  app.post('/api/generate', (req, res) => {
    const body = req.body ?? {};
    const example = safeName(body.example, 'default');
    const exampleDir = join(EXAMPLES_DIR, example);
    if (!existsSync(join(exampleDir, 'site.json'))) {
      res.status(400).json({ error: `Папка с примерами «${example}» не найдена` });
      return;
    }

    const domain = safeName(body.domain, example);
    const outDir = join(OUTPUT_DIR, domain);
    const examplePublic = join(exampleDir, 'public');

    const build = startBuild({
      domain,
      outDir,
      env: {
        SITE_JSON: join(exampleDir, 'site.json'),
        PUBLIC_DIR: existsSync(examplePublic) ? examplePublic : '',
        TEMPLATE: safeName(body.template, ''),
        SCHEME: safeName(body.scheme, ''),
        OUT_DIR: outDir,
        SITE_URL: `https://${domain}`,
        DOMAIN: domain,
        BRAND: String(body.brand ?? ''),
        GEO: String(body.geo ?? ''),
        LOCALE: String(body.locale ?? ''),
        PARTNER_URL: String(body.partnerUrl ?? ''),
      },
    });

    res.json({ buildId: build.id, domain, outDir });
  });

  app.get('/api/builds/:id', (req, res) => {
    const build = builds.get(req.params.id);
    if (!build) {
      res.status(404).json({ error: 'Сборка не найдена' });
      return;
    }
    const { id, status, domain, outDir, lines } = build;
    res.json({ id, status, domain, outDir, lines });
  });

  app.get('/api/builds/:id/log', (req, res) => {
    const build = builds.get(req.params.id);
    if (!build) {
      res.status(404).end();
      return;
    }

    res.writeHead(200, {
      'Content-Type': 'text/event-stream',
      'Cache-Control': 'no-cache',
      Connection: 'keep-alive',
    });

    const send = (line) => res.write(`data: ${JSON.stringify(line)}\n\n`);
    const finish = (status) => {
      res.write(`event: done\ndata: ${JSON.stringify(status)}\n\n`);
      res.end();
    };

    for (const line of build.lines) send(line);

    if (build.status !== 'running') {
      finish(build.status);
      return;
    }

    const listener = { send, finish };
    build.listeners.add(listener);
    req.on('close', () => build.listeners.delete(listener));
  });

  return app;
}

function openBrowser(url) {
  const command =
    process.platform === 'darwin' ? 'open' : process.platform === 'win32' ? 'start' : 'xdg-open';
  try {
    spawn(command, [url], { detached: true, stdio: 'ignore' }).unref();
  } catch {
    // Открытие браузера — удобство, а не условие работы сервера.
  }
}

if (process.argv[1] === fileURLToPath(import.meta.url)) {
  const url = `http://localhost:${PORT}`;
  createApp().listen(PORT, () => {
    console.log(`Фабрика сайтов: ${url}`);
    openBrowser(url);
  });
}
```

- [ ] **Step 4: Запустить тесты сервера**

Run: `npx vitest run tests/server.test.mjs`
Expected: PASS, 7 тестов. Тест сборки занимает несколько секунд — это нормально.

- [ ] **Step 5: Прогнать весь набор**

Run: `npm test`
Expected: PASS.

- [ ] **Step 6: Коммит**

```bash
git add factory/server.mjs tests/server.test.mjs
git commit -m "$(cat <<'MSG'
Сервер фабрики: списки с диска и запуск сборки с потоковым логом

Списки шаблонов, схем и примеров строятся сканированием папок, поэтому новый
шаблон или схема появляются в интерфейсе без правки кода. Имена папок из
браузера проходят через safeName — сборка не может уйти за пределы проекта.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
)"
git push
```

---

### Task 11: Отдача результата — zip, превью, статика интерфейса

**Files:**
- Modify: `factory/server.mjs`
- Test: `tests/server.test.mjs` (дополняется)

**Interfaces:**
- Consumes: `createApp()` (задача 10)
- Produces:
  - `GET /api/output/:domain/zip` — архив собранного сайта, `Content-Type: application/zip`;
  - `GET /preview/:domain/…` — отдача собранного сайта для просмотра в браузере;
  - `GET /` — интерфейс фабрики из `factory/public/`.

- [ ] **Step 1: Дописать падающие тесты**

Добавить в `tests/server.test.mjs` внутрь `describe('factory API', …)`:

```javascript
  it('serves a built site for preview', async () => {
    const response = await fetch(`${base}/preview/api-test.com/`);
    expect(response.status).toBe(200);
    expect(await response.text()).toContain('Find what actually works');
  });

  it('returns a zip of a built site', async () => {
    const response = await fetch(`${base}/api/output/api-test.com/zip`);
    expect(response.status).toBe(200);
    expect(response.headers.get('content-type')).toContain('zip');
    const bytes = new Uint8Array(await response.arrayBuffer());
    expect(bytes.length).toBeGreaterThan(100);
    expect(String.fromCharCode(bytes[0], bytes[1])).toBe('PK');
  });

  it('answers 404 when there is nothing to zip', async () => {
    const response = await fetch(`${base}/api/output/never-built.com/zip`);
    expect(response.status).toBe(404);
  });
```

Эти тесты полагаются на сборку `api-test.com` из предыдущего теста, поэтому должны идти после него — Vitest выполняет тесты внутри `describe` по порядку.

- [ ] **Step 2: Запустить и убедиться в падении**

Run: `npx vitest run tests/server.test.mjs`
Expected: FAIL — маршрутов `/preview` и `/api/output/:domain/zip` ещё нет.

- [ ] **Step 3: Подключить archiver и статику**

В `factory/server.mjs` добавить импорт рядом с остальными:

```javascript
import archiver from 'archiver';
```

И объявить путь к интерфейсу рядом с другими константами:

```javascript
const PUBLIC_UI_DIR = join(HERE, 'public');
```

- [ ] **Step 4: Добавить маршруты в createApp**

Вставить в `createApp()` перед `return app;`:

```javascript
  app.get('/api/output/:domain/zip', (req, res) => {
    const domain = safeName(req.params.domain, '');
    const dir = join(OUTPUT_DIR, domain);
    if (domain === '' || !existsSync(join(dir, 'index.html'))) {
      res.status(404).json({ error: 'Собранного сайта с таким именем нет' });
      return;
    }

    res.attachment(`${domain}.zip`);
    const archive = archiver('zip', { zlib: { level: 9 } });
    archive.on('error', () => res.destroy());
    archive.pipe(res);
    archive.directory(dir, false);
    archive.finalize();
  });

  app.use('/preview', express.static(OUTPUT_DIR, { index: 'index.html' }));
  app.use(express.static(PUBLIC_UI_DIR, { index: 'index.html' }));
```

Порядок важен: `/api/...` объявлены раньше, поэтому статика их не перехватывает.

- [ ] **Step 5: Запустить тесты**

Run: `npx vitest run tests/server.test.mjs`
Expected: PASS, 10 тестов.

- [ ] **Step 6: Прогнать весь набор**

Run: `npm test`
Expected: PASS.

- [ ] **Step 7: Коммит**

```bash
git add factory/server.mjs tests/server.test.mjs
git commit -m "$(cat <<'MSG'
Отдача результата: превью в браузере и архив сборки

Собранный сайт можно открыть по /preview/<домен>/ и скачать zip — это тот же
архив, который позже поедет на хостинг.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
)"
git push
```

---

### Task 12: Интерфейс фабрики

**Files:**
- Create: `factory/public/index.html`, `factory/public/styles.css`, `factory/public/app.js`

**Interfaces:**
- Consumes: API из задач 10 и 11
- Produces: рабочая вкладка «Генерация» и три вкладки-заглушки

**Проверка этой задачи — глазами.** Автотестами покрыт API; интерфейс проверяется вручную по шагам 5–7, потому что тест на «выпадайка заполнилась» требовал бы браузерного окружения, а пользы дал бы меньше, чем один взгляд на экран.

- [ ] **Step 1: Написать разметку**

Файл `factory/public/index.html`:

```html
<!doctype html>
<html lang="ru">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Фабрика сайтов</title>
    <link rel="stylesheet" href="styles.css" />
  </head>
  <body>
    <header class="header">
      <h1>Фабрика сайтов</h1>
    </header>

    <nav class="tabs" id="tabs">
      <button class="tab is-active" type="button" data-tab="generate">Генерация</button>
      <button class="tab" type="button" data-tab="bulk">Массовая генерация</button>
      <button class="tab" type="button" data-tab="download">Скачать с сервера</button>
      <button class="tab" type="button" data-tab="projects">Проекты</button>
    </nav>

    <main>
      <section class="panel is-active" data-panel="generate">
        <p class="hint">Бренд, шаблон, цветовая схема и папка с контентом → готовый сайт в output/</p>

        <form id="generate-form" class="grid">
          <fieldset class="card">
            <legend>Основное</legend>
            <label>Бренд<input name="brand" type="text" placeholder="Example Guide" /></label>
            <label>Гео<input name="geo" type="text" placeholder="ID" /></label>
            <label>Язык<input name="locale" type="text" placeholder="en-US" /></label>
          </fieldset>

          <fieldset class="card">
            <legend>Шаблон и контент</legend>
            <label>Шаблон<select name="template" id="field-template"></select></label>
            <p class="note" id="template-note"></p>
            <label>Цветовая схема<select name="scheme" id="field-scheme"></select></label>
            <label>Папка с примерами<select name="example" id="field-example"></select></label>
            <p class="note">data/examples/[папка]/site.json</p>
          </fieldset>

          <fieldset class="card">
            <legend>Сайт и партнёрка</legend>
            <label>Адрес сайта<input name="domain" type="text" placeholder="example.com" /></label>
            <p class="note">Пустое поле — имя папки берётся от папки с примерами</p>
            <label>Ссылка партнёрки<input name="partnerUrl" type="url" placeholder="https://партнёрка.com/..." /></label>
          </fieldset>

          <fieldset class="card">
            <legend>Результат</legend>
            <p class="status" id="status">Готово к генерации</p>
            <div class="result" id="result" hidden>
              <a class="link" id="result-open" href="#" target="_blank" rel="noopener">Открыть сайт</a>
              <a class="link" id="result-zip" href="#">Скачать zip</a>
            </div>
          </fieldset>
        </form>

        <button class="primary" id="submit" type="submit" form="generate-form">Сгенерировать</button>
        <pre class="log" id="log" aria-live="polite"></pre>
      </section>

      <section class="panel" data-panel="bulk">
        <p class="placeholder">Массовая генерация — в разработке.</p>
      </section>

      <section class="panel" data-panel="download">
        <p class="placeholder">Скачивание с сервера — в разработке.</p>
      </section>

      <section class="panel" data-panel="projects">
        <p class="placeholder">Список проектов — в разработке.</p>
      </section>
    </main>

    <script src="app.js" type="module"></script>
  </body>
</html>
```

- [ ] **Step 2: Написать стили**

Файл `factory/public/styles.css`:

```css
:root {
  --ink: #1b2333;
  --muted: #6b7688;
  --line: #dfe4ec;
  --surface: #f6f8fb;
  --brand: #2f6df6;
  --ok: #167a4a;
  --bad: #b3261e;
  color-scheme: light;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  padding: 0 2rem 4rem;
  background: #fff;
  color: var(--ink);
  font-family: system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif;
  font-size: 14px;
  line-height: 1.55;
}

.header h1 {
  margin: 0;
  padding-block: 1.75rem 1rem;
  font-size: 1.6rem;
}

.tabs {
  display: flex;
  gap: 1.5rem;
  border-bottom: 1px solid var(--line);
}

.tab {
  padding: 0.6rem 0;
  border: 0;
  border-bottom: 2px solid transparent;
  background: none;
  color: var(--muted);
  font: inherit;
  cursor: pointer;
}

.tab.is-active {
  border-bottom-color: var(--brand);
  color: var(--brand);
}

.panel {
  display: none;
  padding-top: 1.5rem;
}

.panel.is-active {
  display: block;
}

.hint,
.note {
  color: var(--muted);
  font-size: 0.85rem;
}

.note {
  margin: -0.4rem 0 0;
}

.grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(15rem, 1fr));
  gap: 1rem;
  margin-block: 1.25rem;
}

.card {
  display: grid;
  gap: 0.85rem;
  align-content: start;
  margin: 0;
  padding: 1.1rem 1.25rem 1.4rem;
  border: 1px solid var(--line);
  border-radius: 10px;
}

.card legend {
  padding: 0 0.35rem;
  font-weight: 600;
}

label {
  display: grid;
  gap: 0.3rem;
  font-size: 0.85rem;
  color: var(--muted);
}

input,
select {
  padding: 0.5rem 0.6rem;
  border: 1px solid var(--line);
  border-radius: 6px;
  background: #fff;
  color: var(--ink);
  font: inherit;
}

input:focus,
select:focus {
  outline: 2px solid var(--brand);
  outline-offset: 1px;
}

.primary {
  padding: 0.65rem 1.5rem;
  border: 0;
  border-radius: 6px;
  background: var(--brand);
  color: #fff;
  font: inherit;
  font-weight: 600;
  cursor: pointer;
}

.primary[disabled] {
  opacity: 0.55;
  cursor: progress;
}

.status {
  margin: 0;
  padding: 0.65rem 0.85rem;
  border-radius: 6px;
  background: var(--surface);
}

.status.is-ok {
  color: var(--ok);
}

.status.is-bad {
  color: var(--bad);
}

.result {
  display: flex;
  gap: 1rem;
}

.link {
  color: var(--brand);
}

.log {
  margin-top: 1.25rem;
  padding: 1rem;
  max-height: 22rem;
  overflow: auto;
  border-radius: 8px;
  background: #10161f;
  color: #d7e0ec;
  font-size: 12px;
  white-space: pre-wrap;
}

.log:empty {
  display: none;
}

.placeholder {
  padding: 3rem;
  border: 1px dashed var(--line);
  border-radius: 10px;
  color: var(--muted);
  text-align: center;
}
```

- [ ] **Step 3: Написать скрипт**

Файл `factory/public/app.js`:

```javascript
const form = document.querySelector('#generate-form');
const submitButton = document.querySelector('#submit');
const statusLine = document.querySelector('#status');
const logBox = document.querySelector('#log');
const result = document.querySelector('#result');
const resultOpen = document.querySelector('#result-open');
const resultZip = document.querySelector('#result-zip');
const templateNote = document.querySelector('#template-note');

function setStatus(text, kind = '') {
  statusLine.textContent = text;
  statusLine.className = kind ? `status is-${kind}` : 'status';
}

function appendLog(line) {
  logBox.textContent += `${line}\n`;
  logBox.scrollTop = logBox.scrollHeight;
}

function fillSelect(select, values) {
  select.replaceChildren(
    ...values.map((value) => {
      const option = document.createElement('option');
      option.value = value.id ?? value;
      option.textContent = value.name ?? value;
      return option;
    }),
  );
}

document.querySelector('#tabs').addEventListener('click', (event) => {
  const tab = event.target.closest('.tab');
  if (!tab) return;
  for (const button of document.querySelectorAll('.tab')) {
    button.classList.toggle('is-active', button === tab);
  }
  for (const panel of document.querySelectorAll('.panel')) {
    panel.classList.toggle('is-active', panel.dataset.panel === tab.dataset.tab);
  }
});

async function loadLists() {
  try {
    const [templates, schemes, examples] = await Promise.all([
      fetch('/api/templates').then((r) => r.json()),
      fetch('/api/schemes').then((r) => r.json()),
      fetch('/api/examples').then((r) => r.json()),
    ]);

    fillSelect(document.querySelector('#field-template'), templates.templates);
    fillSelect(document.querySelector('#field-scheme'), schemes.schemes);
    fillSelect(document.querySelector('#field-example'), examples.examples);

    const describe = () => {
      const chosen = templates.templates.find(
        (item) => item.id === document.querySelector('#field-template').value,
      );
      templateNote.textContent = chosen?.description ?? '';
    };
    document.querySelector('#field-template').addEventListener('change', describe);
    describe();
  } catch (error) {
    setStatus(`Не удалось загрузить списки: ${error.message}`, 'bad');
  }
}

function followBuild(buildId, domain) {
  const stream = new EventSource(`/api/builds/${buildId}/log`);

  stream.addEventListener('message', (event) => appendLog(JSON.parse(event.data)));

  stream.addEventListener('done', (event) => {
    const status = JSON.parse(event.data);
    stream.close();
    submitButton.disabled = false;

    if (status === 'ok') {
      setStatus(`Готово: output/${domain}`, 'ok');
      resultOpen.href = `/preview/${domain}/`;
      resultZip.href = `/api/output/${domain}/zip`;
      result.hidden = false;
    } else {
      setStatus('Сборка не удалась — смотри лог', 'bad');
    }
  });

  stream.addEventListener('error', () => {
    stream.close();
    submitButton.disabled = false;
    setStatus('Связь с сервером прервалась', 'bad');
  });
}

form.addEventListener('submit', async (event) => {
  event.preventDefault();
  submitButton.disabled = true;
  logBox.textContent = '';
  result.hidden = true;
  setStatus('Собираем…');

  try {
    const payload = Object.fromEntries(new FormData(form).entries());
    const response = await fetch('/api/generate', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(payload),
    });
    const data = await response.json();

    if (!response.ok) {
      setStatus(data.error ?? 'Сервер отклонил запрос', 'bad');
      submitButton.disabled = false;
      return;
    }

    followBuild(data.buildId, data.domain);
  } catch (error) {
    setStatus(`Ошибка запроса: ${error.message}`, 'bad');
    submitButton.disabled = false;
  }
});

loadLists();
```

- [ ] **Step 4: Запустить фабрику**

Run: `npm run dev`
Expected: в терминале строка `Фабрика сайтов: http://localhost:3002`, браузер открывается сам.

- [ ] **Step 5: Проверить заполнение списков**

Ожидаемо на вкладке «Генерация»: «Шаблон» содержит Классический / Разделённый / Компактный, под ним описание выбранного; «Цветовая схема» — blue / dark / green; «Папка с примерами» — broken / default.

- [ ] **Step 6: Проверить генерацию**

Заполнить: Бренд `Проверка`, Шаблон `Разделённый`, Схема `green`, Папка `default`, Адрес `check.local`, Партнёрка `https://example.org/go`. Нажать «Сгенерировать».

Ожидаемо: статус меняется на «Собираем…», в чёрном логе появляются строки сборки, затем статус «Готово: output/check.local» зелёным и две ссылки. «Открыть сайт» показывает собранный сайт, кнопки в hero ведут на партнёрскую ссылку. «Скачать zip» скачивает архив.

- [ ] **Step 7: Проверить вкладки-заглушки**

Переключиться на «Массовая генерация», «Скачать с сервера», «Проекты» — на каждой рамка с надписью «в разработке». Вернуться на «Генерация»: лог и результат предыдущей сборки на месте.

- [ ] **Step 8: Прогнать весь набор**

Run: `npm test`
Expected: PASS.

- [ ] **Step 9: Коммит**

```bash
git add factory/public
git commit -m "$(cat <<'MSG'
Интерфейс фабрики: вкладка генерации и три заглушки

Списки шаблонов, схем и примеров приходят с сервера, лог сборки читается
через EventSource и показывается живьём. Нерабочие вкладки честно помечены
как «в разработке».

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
)"
git push
```

---

### Task 13: Приёмка этапа

**Files:**
- Create: `scripts/check-matrix.mjs`, `README.md`

**Interfaces:**
- Consumes: всё предыдущее
- Produces: команда `npm run check:matrix`, документация запуска

- [ ] **Step 1: Написать скрипт проверки матрицы**

Файл `scripts/check-matrix.mjs`. Девять сборок — это минуты, поэтому скрипт запускается руками, а не в `npm test`.

```javascript
import { execFileSync } from 'node:child_process';
import { existsSync, readFileSync, rmSync } from 'node:fs';
import { join } from 'node:path';
import { listTemplates } from '../src/lib/templates.mjs';
import { listSchemes } from '../src/lib/schemes.mjs';

const ASTRO_BIN = join('node_modules', '.bin', 'astro');
const EXAMPLE = process.argv[2] || 'default';
const EXAMPLE_DIR = join('data', 'examples', EXAMPLE);

if (!existsSync(join(EXAMPLE_DIR, 'site.json'))) {
  console.error(`Нет примера «${EXAMPLE}» в data/examples/`);
  process.exit(1);
}

const templates = listTemplates();
const schemes = listSchemes();
const failures = [];

console.log(`Проверка ${templates.length} × ${schemes.length} на примере «${EXAMPLE}»\n`);

for (const template of templates) {
  for (const scheme of schemes) {
    const outDir = join('output', 'matrix', `${template.id}-${scheme}`);
    rmSync(outDir, { recursive: true, force: true });
    const label = `${template.id} × ${scheme}`;

    try {
      const examplePublic = join(EXAMPLE_DIR, 'public');
      execFileSync(ASTRO_BIN, ['build'], {
        stdio: 'pipe',
        env: {
          ...process.env,
          SITE_JSON: join(EXAMPLE_DIR, 'site.json'),
          PUBLIC_DIR: existsSync(examplePublic) ? examplePublic : '',
          TEMPLATE: template.id,
          SCHEME: scheme,
          OUT_DIR: outDir,
          SITE_URL: 'https://example.com',
        },
      });

      const html = readFileSync(join(outDir, 'index.html'), 'utf8');
      const problems = [];
      if (!html.includes('--c-primary')) problems.push('нет переменных схемы');
      if (/<script[^>]*\ssrc=/i.test(html)) problems.push('в выходе есть JS');
      if (!html.includes('Find what actually works') && EXAMPLE === 'default') {
        problems.push('нет контента hero');
      }

      if (problems.length > 0) {
        failures.push(`${label}: ${problems.join(', ')}`);
        console.log(`  ✗ ${label} — ${problems.join(', ')}`);
      } else {
        console.log(`  ✓ ${label}`);
      }
    } catch (error) {
      failures.push(`${label}: сборка упала`);
      console.log(`  ✗ ${label} — сборка упала`);
      console.log(String(error.stderr ?? error.message).trim());
    }
  }
}

console.log('');
if (failures.length > 0) {
  console.error(`Провалов: ${failures.length}`);
  process.exit(1);
}
console.log('Все комбинации собрались.');
```

- [ ] **Step 2: Запустить проверку матрицы**

Run: `npm run check:matrix`
Expected: девять строк с галочками и «Все комбинации собрались.». Занимает несколько минут.

- [ ] **Step 3: Написать README**

Файл `README.md`:

````markdown
# Фабрика сайтов

Локальный конвейер: форма в браузере → готовый статический сайт в `output/<домен>/`.

## Требования

Node.js ≥ 22.12.0 (чётные версии; v23 не поддерживается).

## Запуск

```bash
npm install
npm run dev
```

Открывается `http://localhost:3002`. На вкладке «Генерация» выбираются шаблон,
цветовая схема и папка с контентом; кнопка «Сгенерировать» собирает сайт и
показывает лог живьём. Готовое открывается по ссылке или скачивается архивом.

## Сборка без интерфейса

```bash
SITE_JSON=data/examples/default/site.json \
PUBLIC_DIR=data/examples/default/public \
TEMPLATE=t2 SCHEME=green OUT_DIR=output/example.com \
SITE_URL=https://example.com \
npm run build:site
```

Переменные: `SITE_JSON` (контент), `PUBLIC_DIR` (картинки), `TEMPLATE`, `SCHEME`,
`OUT_DIR`, `SITE_URL`, а также необязательные `BRAND`, `GEO`, `LOCALE`, `DOMAIN`,
`PARTNER_URL` — они перекрывают одноимённые поля контента.

## Тесты

```bash
npm test              # весь набор
npm run check:matrix  # все шаблоны × все схемы, несколько минут
```

## Как это устроено

Три слоя с жёсткими границами:

- `factory/` — Express-сервер и интерфейс. Знает про папки и команды, не знает про вёрстку.
- `src/` — движок на Astro. Чистая функция «контент + шаблон + схема → папка сайта».
- `templates/tN/` — шаблоны-плагины: `manifest.json` и `.astro`-компоненты блоков.

### Добавить шаблон

Положить папку `templates/t4/` с `manifest.json` и файлами блоков — по одному на
тип из `blocks`, имя файла совпадает с типом. Интерфейс подхватит шаблон сам;
править код движка не нужно.

### Добавить цветовую схему

Положить `styles/schemes/<имя>.css` с тем же набором `:root`-переменных, что и в
существующих схемах. Тест проверяет, что наборы совпадают.

### Контент

`data/examples/<папка>/site.json`, картинки — в `data/examples/<папка>/public/`.
Валидации нет: недостающие поля получают значения по умолчанию, неизвестные блоки
отбрасываются с предупреждением в лог. Сборка не падает из-за контента.

## Что дальше

Заливка на сервер по кнопке, вкладка «Проекты», массовая генерация, PWA,
верификация Google, медиа-пайплайн. Подробности — в
`docs/specs/2026-09-07-factory-v1-design.md`.
````

- [ ] **Step 4: Проверить критерии готовности вручную**

Пройти по списку из спека, отмечая каждый пункт:

1. `npm run dev` поднимает интерфейс и открывает браузер.
2. Выпадайки заполнены списками с диска: три шаблона, три схемы, две папки примеров.
3. «Сгенерировать» собирает сайт в `output/<домен>/`, лог виден живьём.
4. Готовый сайт открывается по ссылке и скачивается архивом.
5. Три шаблона визуально различимы на одном контенте — проверить, собрав `default` каждым.
6. Три схемы работают на каждом шаблоне — `npm run check:matrix`.
7. Битый контент даёт сайт с пропущенными блоками: собрать пример `broken`, убедиться, что страница открывается, а в логе есть строки про пропущенные блоки.
8. `npm test` проходит целиком.

- [ ] **Step 5: Прогнать весь набор в последний раз**

Run: `npm test`
Expected: PASS.

- [ ] **Step 6: Коммит**

```bash
git add scripts/check-matrix.mjs README.md
git commit -m "$(cat <<'MSG'
Приёмка этапа: проверка матрицы и README

check:matrix прогоняет все шаблоны на всех схемах и проверяет три вещи:
сборка не упала, переменные схемы на месте, JS в выходе нет.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
)"
git push
```

---

## Критерии готовности этапа

- `npm test` проходит целиком.
- `npm run check:matrix` — девять комбинаций собираются без ошибок.
- `npm run dev` открывает интерфейс на `localhost:3002`, списки заполнены с диска.
- Кнопка «Сгенерировать» собирает сайт, лог виден живьём, результат открывается и скачивается.
- Три шаблона дают разную разметку на одном и том же контенте.
- Битый контент даёт сайт без проблемных блоков и предупреждения в логе, а не отказ сборки.
- В выходном HTML нет подключённых JS-бандлов.
- Строки `t1`, `t2`, `t3` не встречаются в `src/` — проверяется командой `grep -rn "t[123]" src/ || echo "чисто"`.
