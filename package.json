{
  "name": "site-factory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node factory/server.mjs",
    "build:site": "astro build",
    "test": "vitest run",
    "check:matrix": "node scripts/check-matrix.mjs"
  },
  "dependencies": {
    "archiver": "^8.0.0",
    "astro": "^7.3.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "vitest": "^5.0.0"
  }
}
